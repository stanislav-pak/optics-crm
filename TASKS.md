# TASKS — optics-crm

## Как использовать
Каждая сессия = одна задача.
Начинай сессию: «Прочитай TASKS.md и выполни первую задачу со статусом TODO»
После выполнения статус меняется на DONE.

> **Следующая задача: T66 — ждёт проверки на проде, увидит ли пользователь понятную ошибку при повторном «Отправить» (если да — гипотеза RLS подтверждена, нужно чинить саму политику). После неё — T65 (ЗАВЕРШИТЬ ОТЛАДКУ ПЕЧАТИ ЭТИКЕТОК, спросить пользователя о результате первым делом), затем T23/T62/T63.**

Статусы: `TODO` | `IN_PROGRESS` | `DONE` | `SKIP`

---

### T78 — Касса не пересчитывалась + возврат всегда из наличных `DONE` (2026-08-11)
**Баг с прода:** менеджер «Гум» погасила долг 3000 ₸ по Kaspi — касса осталась 20 500 вместо 23 500.

**Корень (не в формулах!):** и `CashSessionCard`, и `cashAdmin` правильно читают погашенные долги и кладут их в нужный карман. Проблема в том, что `cash_sessions.system_*` — это **кэш**, который перезаписывается только при загрузке `CashSessionCard` с открытой сменой. Обновляли кассу лишь два события: `workshop-payment-accepted` и `sale-returned`. Погашение долга, новая продажа и расход не обновляли ничего.

**Доказательство из базы:** Абая 34 — долг Руслана 20 000 погашен в 22:33, касса закрыта в 22:34, `system_kaspi = 152 600 = 132 600 продаж + 20 000` → **попало** (успели перезагрузить). Гум — долг Мариям 3000 погашен в 23:59, касса закрыта в 00:02, `system_kaspi = 20 500` = ровно сумма продаж, без долга → **не попало**. Разница между филиалами и доказывает, что дело в пересчёте, а не в формуле.

**Усугубляло:** `close_cash_session` берёт суммы из строки смены как есть и не пересчитывает — устаревшая цифра замерзала в `cash_session_closures` навсегда.

**Сделано (3 фикса):**
1. Новый модуль `services/cashEvents.ts` — событие `cash-changed` + `notifyCashChanged()`. Механизм тот же, что уже работал для доплаты мастерской. Подключено к: погашению долга (`handleSettleDebt`), созданию продажи (`onSuccess` у `AddSaleModal`), созданию и удалению расхода (`ExpensesTab`). Слушатель — в самом `CashSessionCard`, рядом с существующим `sale-returned`.
2. Принудительный пересчёт перед закрытием: `handleClose` вызывает `loadSession(true)` до `closeCashSession`. Флаг `silent` добавлен, чтобы модал закрытия не подменился спиннером. Гонка «сделал операцию → не обновилось → заморозил» закрыта.
3. Возврат товара теперь вычитается из того кармана, которым платили. Логика вынесена в новый `services/cashCalc.ts` и используется и кассой филиала, и админ-кассой (раньше обе всегда минусовали наличные). Смешанная оплата делится **пропорционально фактическим `paid_*`** исходной продажи; вычитаем не больше, чем реально получено (продажа с долгом). В `AdminCashData` добавлено поле `returnsKaspi`, в админ-вид — строка «Kaspi QR» в блоке возвратов.

**Проверено:** 115/115 тестов (+20 новых), `tsc` 0 ошибок, `npm run build` ок. Тесты ловят регрессии — проверено вручную: без пересчёта перед закрытием падает `expected 1 to be >= 2`.

**Данные не трогали.** Смену Гум за 10.08 владелец переоткроет сам после деплоя — при переоткрытии она пересчитается и заберёт недостающие 3000 ₸.

**Известное ограничение (осознанно):** если по одной продаже был и погашенный долг, и возврат — возврат вычтется не полностью, т.к. погашенный долг не входит в пропорцию (у него своя дата и свой способ оплаты). Стык редкий, вынесен отдельно.

**Не входило в скоуп:** предзаказные баги (T79), возврат доплаты мастерской в админ-кассе, Kaspi-расходы.

**Файлы:** `services/cashEvents.ts` (новый), `services/cashCalc.ts` (новый), `components/Inventory/CashSessionCard.tsx`, `components/Inventory/ExpensesTab.tsx`, `pages/InventoryPage.tsx`, `services/cashAdmin.ts`, `components/Admin/AdminCashView.tsx`, `test/cashCalc`-тесты (2 новых файла).

---

### T80 — Закрытие остальных денежных багов (предзаказы, POST/перевод, расходы Kaspi) `DONE` (2026-08-11)
Пять пунктов «довести кассу до 100%».

**1. Предзаказы (был T79).** Превращения предзаказа в продажу в системе НЕТ — `OrdersListView` только двигает статусы, а при поступлении товара заказ становится `completed`. Поэтому деньги учитываются прямо из `orders`, как у мастерской из `service_orders`: новый запрос по `prepayment_paid_at` + `prepayment_method`, отменённые заказы исключены. Плюс: `createOrder` теперь проставляет `prepayment_paid_at` (без него запрос не нашёл бы ничего), а `payment_type='full'` пишет всю сумму в `prepayment_amount` вместо 0. Задвоения нет, потому что продажи под предзаказом не создаётся — проверено, единственное обращение к `orders` в `AddSaleModal` только привязывает заявку на склад. Добавлен `notifyCashChanged()` в путь предзаказа (он идёт мимо `onSuccess`).

**2. Возврат доплаты мастерской в админ-своде.** Добавлен запрос по `remaining_refunded_at` и вычет — раньше касса филиала вычитала, а админка нет, и две витрины расходились.

**3. Расходы по Kaspi в кассе филиала.** Считаются и показываются строкой «Расходы по Kaspi» + «Kaspi за вычетом расходов». Как и в админке, вычет на уровне отображения: `system_cash`/`system_kaspi` остаются «грязными», расходы наличными вычитает функция закрытия на сервере.

**4. POST и Kaspi-перевод для долга и доплаты.** Миграция `20260811_payment_methods_halyk_transfer.sql` расширяет CHECK у `sales.debt_payment_method`, `service_orders.remaining_payment_method` и `orders.prepayment_method` до 4 значений и обновляет валидацию внутри `settle_sale_debt`. В интерфейсе 4 кнопки вместо 2 — в диалоге погашения долга, в диалоге доплаты и в `PendingPaymentsView`. **Возврат предоплаты оставлен на 2 способах** — `prepayment_refund_method` не расширяли, диалог там общий, поэтому набор кнопок зависит от типа операции.

**5. Граница дня кассы — НЕ трогали, сознательно.** Проверено по бою: продаж после 19:00 UTC ноль, все 106 продаж укладываются в 09:00–20:00 по Алматы, погашения долгов (22:33 и 23:59 местного) тоже не пересекают границу — то есть переход на алматинскую зону не сдвинул бы ни одной записи. Но менять всё равно не стали: **текущая граница проходит в 05:00 по Алматы, и это лучше полуночи**. Смену за 10.08 закрывали в 00:02 местного — при переходе на полночь этот вечер разъехался бы на две смены. Оставлено как есть; если менять — то на «границу в 05:00 по Алматы» явно, а не на календарную полночь.

**Общее:** повторяющиеся `filter(m==='cash').reduce(...)` заменены на `sumByPaymentMethod` в `cashCalc.ts` — раньше они знали только про наличные и Kaspi, из-за чего POST/перевод терялись. Формулы кассы в обоих местах сведены к «приход по карману минус возвраты по нему же».

**Проверено:** 125/125 тестов (+10 новых), `npm run build` ок, типы — **50 ошибок до правок и 50 после** (ноль новых; проверено через `git stash`).

**ВАЖНО про проверку типов:** `npx tsc --noEmit` в этом проекте **ничего не проверяет** (корневой конфиг — solution-файл). Правильно: `npx tsc --noEmit --project tsconfig.app.json`, сверять count до/после. Это уже было записано в PROJECT_MEMORY, но в задачах T74–T78 я отчитывался «tsc 0 ошибок» именно голой командой — те отчёты по типам были пустыми (сборка и тесты в них настоящие).

**Файлы:** `supabase/migrations/20260811_payment_methods_halyk_transfer.sql` (новый), `services/cashCalc.ts`, `services/cashAdmin.ts`, `services/orders.ts`, `services/inventory.ts`, `components/Inventory/CashSessionCard.tsx`, `components/Inventory/AddSaleModal.tsx`, `components/Admin/AdminCashView.tsx`, `components/Workshop/PendingPaymentsView.tsx`, `pages/InventoryPage.tsx`, `types/index.ts`, `test/cashPreordersAndMethods.test.ts` (новый), правки заглушек в двух старых тестах.

**Осталось (мелочь, не брали):** способ возврата предоплаты мастерской — только наличные/Kaspi; `AddSaleModal.tsx:585` пишет способ предоплаты мастерской как «наличные» для всего, кроме Kaspi QR (T77).

---

### T79 — Деньги предзаказа не попадают в кассу `DONE` (2026-08-11, в составе T80)
**Найдено при аудите (T78).** Предзаказ создаёт только строку в `orders`; ни касса филиала, ни админ-касса таблицу `orders` не читают вовсе. Ссылки на продажу у заказа нет, автоматического превращения в продажу тоже — при поступлении товара заказ просто становится `completed`.

Два случая:
- **предоплата** — сумма лежит в `orders.prepayment_amount`, в кассу не попадает;
- **100% оплата** — `prepayment_amount` пишется **0** (`AddSaleModal.tsx:413` ставит сумму только для `prepaid`), продажи нет → деньги не записаны нигде вообще.

**Пока не стрельнуло:** предзаказов в базе 0, функцией не пользовались. Решить, как учитывать: отдельной строкой в кассе по `orders.prepayment_paid_at` или созданием продажи в момент оплаты.

---

### T76 — Задвоение суммы мастерской в кассе `DONE` (2026-08-10)
**Баг с прода:** менеджер «Гум» пробила ремонт на 500 ₸ — касса показала 1000 ₸.

**Корень:** заказ мастерской, оформленный ВНУТРИ продажи (галочка «в мастерскую» в `AddSaleModal`), кладёт одну и ту же сумму в два места: в `paid_cash`/`paid_kaspi` самой продажи (`totalNow = товары + мастерская`, `AddSaleModal.tsx:516-519`) и в `prepayment` заказа с отметкой `prepayment_paid_at` (`AddSaleModal.tsx:571-587`). Касса суммировала оба.

**Доказательство:** продажа `cb9fe2b9` 10.08 10:00 — `total = 0`, `paid_kaspi = 500`; связанный заказ `f1830764` «Замена носоупоров» — `prepayment = 500`, метод kaspi, `payment_type = full`. Смена «Гум» 10.08: `system_kaspi = 21 000`, по продажам реально 8000+12000+500 = 20 500. Разница ровно 500.

**Масштаб (проверен по всей базе):** заказ мастерской в базе был ВСЕГО ОДИН — модуль только начали использовать. Затронута 1 смена из 29, 1 филиал из 7, на 500 ₸, только Kaspi. Испорченных записей закрытия — 0 из 13, ложных недостач — 0. Баг при этом системный (в логике), а не в данных одной смены.

**Двоят оба денежных пути** (из кода): 100%-оплата — вся сумма, предоплата — на величину предоплаты, и в наличных, и в Kaspi. «Оплата при получении» не двоит (предоплата 0). Доплата при выдаче считается верно — она нигде, кроме заказа, не фиксируется.

**Сделано:** в обоих местах подсчёта предоплат мастерской добавлен фильтр `.is('sale_id', null)` — считаем только заказы БЕЗ привязанной продажи. Заказ с продажей уже учтён в самой продаже; заказ, созданный напрямую через мастерскую, продажи не имеет и считается как раньше. Запрос доплат при выдаче не тронут. БД/историю не трогали — правка только на чтение, миграций нет.

**Проверено:** 99/99 тестов (+4 новых), `tsc` 0 ошибок, `npm run build` ок. Новый тест `test/cashAdminWorkshopPrepayment.test.ts` проверяет оба направления (заказ с продажей не считается, без продажи считается) и что фильтр не задел запрос доплат. Убедился, что тест ловит регрессию: с временно убранным фильтром падает на 3700 вместо 3000.

**Важно про запись данных:** `close_cash_session` берёт суммы из строки смены КАК ЕСТЬ и не пересчитывает — то есть при закрытии неверная цифра замёрзла бы в `cash_session_closures` навсегда. Плюс недостача считается от `system_cash`, так что задвоение в наличных дало бы ложную недостачу и авто-событие «кассовое расхождение» на сотрудника. Успели до закрытия: смена «Гум» на момент фикса была открыта, закрытий по ней 0.

**Не входило в скоуп (отдельная задача — T77):** способ предоплаты определяется как «kaspi, если оплата шла через Kaspi QR, иначе наличные» (`AddSaleModal.tsx:585`) — оплата через Halyk/перевод/смешанную записывается в заказе как наличные.

**Файлы:** `components/Inventory/CashSessionCard.tsx`, `services/cashAdmin.ts`, `test/cashAdminWorkshopPrepayment.test.ts` (новый).

---

### T77 — Способ предоплаты мастерской: Halyk/перевод пишется как наличные `TODO`
**Найдено при T76.** `AddSaleModal.tsx:585`: `prepayment_method = paymentMethod === 'kaspi_qr' ? 'kaspi' : 'cash'`. При оплате заказа мастерской через Halyk, Kaspi-перевод или смешанно в заказ пишется `cash`.

**Влияние после T76:** на кассу больше не влияет — такие заказы (с `sale_id`) из подсчёта предоплат исключены. Остаётся враньё в самом заказе и в отчётах по мастерской.

**Что сделать:** передавать реальный способ оплаты; для смешанной оплаты решить, как делить (возможно, брать метод, которым покрыта часть мастерской). Проверить, что заказы, созданные напрямую через `AddServiceOrderModal`, метод ставят корректно.

---

### T74 — Менеджер не мог погасить долг по продаже (RLS) `DONE` (2026-08-10)
**Баг с прода:** менеджер (филиал «Абая 34») жмёт «Погасить долг ₸20 000» у клиента Руслан — ошибка «Не удалось погасить долг — возможно, уже погашен, либо нет прав на изменение».

**Корень:** RLS-политика `employees_update_sales` разрешает не-админу UPDATE строки `sales` только при `status = 'pending'`. Продажа с частичной оплатой создаётся сразу со `status = 'paid'` (долг живёт в `debt_amount`, не в `status` — см. миграцию 20260805). UPDATE задевал 0 строк, `.single()` превращал это в ошибку. Проверено по бою: `debt_paid_at` = NULL (долг реально не погашен, не «завис UI»), филиал совпадает — блокировал именно статус. В филиале «Абая 34» все 3 сотрудника — `manager`, т.е. погасить не мог никто, кроме админа. Затронуто 2 продажи: Руслан 20 000 и Мариям 3 000.

**Сделано:** миграция `20260810_settle_sale_debt_rpc.sql` — функция `public.settle_sale_debt(p_sale_id, p_method)` SECURITY DEFINER (тот же приём, что у возврата). Проверки внутри: `p_method in ('cash','kaspi')`, вызывающий — активный сотрудник, не-админ только свой филиал, `debt_amount > 0`, `debt_paid_at IS NULL`; UPDATE меняет ровно 2 колонки с условием `and debt_paid_at is null` (защита от гонки). `grant execute to authenticated`, `revoke from anon`. **RLS-политики `sales` НЕ трогали** — расширять их на `status='paid'` было бы дырой: менеджер получил бы право править у оплаченной продажи любое поле, включая суммы. Фронт: `settleSaleDebt` в `services/inventory.ts` переписан на `supabase.rpc`, ошибки БД переводятся в понятный текст («Долг уже погашен» / «Недостаточно прав» / «По этой продаже нет долга» / «Продажа не найдена»).

**Применено на бой:** да (DDL через Management API). Проверено по каталогу: `prosecdef = true`, `search_path = public, pg_temp`, execute у `authenticated`, у `anon` нет. **Ни один реальный долг не погашен** — обе продажи остались `debt_paid_at = NULL`, гасит менеджер сам.

**Проверено:** 95/95 тестов зелёных, `tsc --noEmit` — 0 ошибок, `npm run build` — ок. Тест-файл `test/settleSaleDebt.test.ts` переписан под RPC (7 тестов: payload не содержит сумм, оба способа оплаты, все 4 маппинга ошибок + незнакомая ошибка не проглатывается). В `test/setup.ts` добавлен `rpc` в общий мок supabase.

**Не исправлено, вынесено отдельно:** у существующей `update_sale_status_for_return` (возврат товара) проверок роли/филиала нет вообще — любой залогиненный может оформить возврат по чужой продаже в чужом филиале. Обнаружено при чтении её как образца. Отдельная задача — T75.

**Файлы:** `supabase/migrations/20260810_settle_sale_debt_rpc.sql` (новый), `services/inventory.ts`, `test/settleSaleDebt.test.ts`, `test/setup.ts`.

---

### T75 — `update_sale_status_for_return` без проверки прав `DONE` (2026-08-10)
**Найдено при T74.** Функция `public.update_sale_status_for_return(p_sale_id, p_new_status)` — SECURITY DEFINER, т.е. обходит RLS, но внутри проверяла только допустимость статуса. Ни роли, ни филиала, ни «активный сотрудник». **Хуже, чем казалось при первом описании:** EXECUTE был выдан `PUBLIC` и `anon`, т.е. перевести любую продажу в `refunded`/`partially_refunded` мог кто угодно с публичным anon-ключом, даже не залогинившись.

**Анализ перед правкой** (учли урок T74, где RLS молча ломала штатный сценарий — сначала убедились, что guard никого легитимного не отрежет):
- все 6 возвратов за историю — менеджер в своём же филиале; кросс-филиальных 0, с неизвестным сотрудником 0;
- продажи есть только в «Абая 34» (59) и «Гум» (47); у склада и мастерских — 0, т.е. «складской» менеджер (единственное расширение прав в UI, `WAREHOUSE_ID` в `InventoryPage`) возвраты не делает в принципе;
- пользователей `branch_admin` в базе нет вообще: только `admin` (2) и `manager` (12);
- SELECT-политика `employees_select_sales` и так пускает не-админа только к своему филиалу — чужую продажу он не может даже загрузить; guard лишь повторяет это правило на записи;
- вызывающий один на весь код — `createReturn()` из `ReturnModal`, из браузера под сессией сотрудника; бэкенд/n8n функцию не зовут.

**Сделано:** миграция `20260810_return_rpc_auth_guard.sql` — guard в начало функции по модели `settle_sale_debt`: активный сотрудник, не-админ только свой филиал, иначе `FORBIDDEN`. Бизнес-логика возврата (статусы, движения, остатки) не изменена ни на строку. `SECURITY DEFINER` сохранён, `search_path` ужесточён до `public, pg_temp`. Отозван EXECUTE у `PUBLIC` и `anon`, оставлен `authenticated`. RLS-политики не трогали. Фронт: в `createReturn` ошибка `FORBIDDEN` переводится в «Недостаточно прав для возврата».

**Применено на бой:** да. Проверено по каталогу: guard на месте, `prosecdef = true`, в правах `anon` больше нет. Статусы продаж не изменились (102 paid + 4 refunded — как до правки), **ни одного возврата не оформлено**.

**Проверено:** 95/95 тестов, `tsc --noEmit` — 0 ошибок, `npm run build` — ок.

**Не покрыто тестом:** маппинг `FORBIDDEN` → «Недостаточно прав для возврата» в `createReturn`. Тестов на `createReturn` в проекте нет вообще, а мок всей цепочки (продажа → движения → остатки → 2 RPC) заметно больше самой правки. Кандидат на отдельную задачу вместе с остальным покрытием возвратов.

**Замечено, не исправлено (вне скоупа):** `createReturn` возвращает товар на склад и пишет движения ДО вызова RPC статуса. Если RPC откажет, возврат останется наполовину сделанным (остатки вернулись, статус прежний). В штатном пути guard не срабатывает — до него не дойти без прав на чтение продажи, — но сама последовательность хрупкая; чинится только переносом всего возврата в одну транзакцию/RPC.

---

### T73 — Показать/скрыть пароль (глаз) на всех формах входа `DONE` (2026-08-06)
**Запрос владельца:** на экране входа нет кнопки-глаза для пароля. Фронт-онли.

**Сделано:** общий компонент `components/Auth/PasswordInput.tsx` (Eye/EyeOff, `aria-label` «Показать/Скрыть пароль» с кастомным словом через `ariaSubject`, `tabIndex={-1}`, `type="button"`) — подключён во ВСЕ 5 полей пароля в приложении: `LoginForm` (не было вообще), `SignupForm` (пароль + код руководителя — у пароля был toggle без aria-label, у кода не было), `ResetPasswordForm` (новый пароль + повтор — у повтора кнопки не было вовсе, видимость молча делила state с первым полем). Теперь у каждого поля независимая видимость.

**Проверено:** саб-агент-ревью (проблем не найдено, отдельно проверил что `git grep 'type="password"'` по всему `src/` больше не даёт полей без toggle), 88/88 тестов зелёных (+9 новых), `tsc` — 50 ошибок (база не выросла), `npm run build` — ок.

**Файлы:** `components/Auth/PasswordInput.tsx` (новый), `LoginForm.tsx`, `SignupForm.tsx`, `ResetPasswordForm.tsx`, `test/passwordInput.test.tsx`, `test/loginForm.test.tsx`.

**Коммит:** `b4e5374`, запушен в `main` (прод и тест — одна ветка).

---

### T72 — Единый отчёт продаж в AdminSalesHistory (вкладка «Продажи») `DONE` (2026-08-06)
**Запрос владельца:** по разбору (см. T71 выше — там же проверялось, что уже видит админ) не хватало: времени продажи, цены/скидки по позициям, сводки по филиалу+периоду и пагинации свыше 200 записей. Фронт-онли, без миграций.

**Сделано:**
- Дата+время (было только дата) через `fmtDateTime`.
- Позиции с ценой за штуку и «было/скидка» (переиспользована логика из `InventoryPage.tsx`).
- Сводка за филиал+период: кол-во продаж, Σ `sales.total` по не-возвратным (`status !== 'refunded'`), «собрано», «долг» — отдельный независимый запрос, не зависит от того, сколько карточек подгружено пагинацией.
- Курсорная пагинация (created_at+id, 50/страница, «Показать ещё») вместо тихого `.limit(200)` — по образцу `ActivityAuditView.tsx` (T71).

**Найден и исправлен баг до коммита (саб-агент-ревью):** guard от гонки состояний (`requestSeqRef`) защищал только список продаж, но не сводку/предзаказы/мастерскую — при быстрой смене фильтра могла применяться устаревшая сводка поверх свежего списка. Исправлено: подфункции стали чистыми (возвращают данные), все `setState` централизованы в `loadAll()` под одной проверкой. Добавлен регрессионный тест на этот сценарий. Повторное ревью подтвердило фикс.

**Осознанное решение (не баг):** в сводке возвратом считается только `status === 'refunded'`; `partially_refunded` продажи по-прежнему считаются по исходной `sales.total` (частичный возврат не корректирует total/paid_* — так было и в карточках раньше). Если владелец захочет вычитать частичные возвраты из сводки — отдельная задача.

**Проверено:** саб-агент-ревью (2 захода — нашёл и подтвердил фикс гонки состояний), 79/79 тестов зелёных (+9 новых), `tsc` — 50 ошибок (база не выросла), `npm run build` — ок.

**Файлы:** `components/Admin/AdminSalesHistory.tsx`, `test/adminSalesHistory.test.tsx`.

**Коммит:** `84a9cc0`, запушен в `main` (прод и тест — одна ветка).

---

### T71 — Журнал действий (activity_audit), разрез по филиалам `DONE` (2026-08-06)
**Запрос владельца:** общий CRUD-аудит "кто что сделал" для роли admin, отдельно от уже существующих `activity_log` (CRM-активность) и `watchlist_events` (6 типов рискованных событий).

**Сделано:**
- Таблица `activity_audit` (кто/когда/действие/сущность/`branch_id`/old_data/new_data jsonb/source) + индексы + RLS (читать только admin, писать может только триггер).
- Один общий `SECURITY DEFINER` триггер `log_activity_audit()` на 9 таблицах: products, sales, employees, branches, stock_requests, expenses, clients, service_orders, revisions — на уровне "шапки", не построчно (sale_items/stock_movements/revision_items не аудируются, у них своя история).
- `branch_id` определяется из самой строки; для `branches` — сама сущность и есть филиал; иначе — филиал актёра. "Кто" — через `employees.user_id = auth.uid()`; пусто (service-role/RPC) → `source='system'`.
- **Устойчивость:** `EXCEPTION WHEN OTHERS THEN RETURN COALESCE(NEW, OLD)` — сбой записи в аудит не ломает основную операцию (продажу, правку товара и т.д.).
- Экран `ActivityAuditView.tsx` в панели админа («Журнал действий», рядом с «Историей продаж»): фильтры филиал/сотрудник/действие/сущность/даты, курсорная пагинация «Показать ещё», человекочитаемое summary («цена: 12 000→9 000») из `activityAuditSummary.ts`, raw JSON по клику «Подробнее».

**Найдено ревью до коммита:** миграция была не идемпотентна (`CREATE POLICY` без `DROP POLICY IF EXISTS` — упала бы при повторном применении). Исправлено, плюс добавлена защита от гонки состояний при быстром переключении фильтров и `pg_temp` в `search_path` функции.

**Проверено:** саб-агент-ревью (нашёл и подтверждён фикс проблемы выше; отдельно подтвердил оба ключевых требования — `branches` не ломает функцию, `EXCEPTION` реально не роняет операцию), 70/70 тестов зелёных (+22 новых), `tsc` — 50 ошибок (база не выросла), `npm run build` — ок.

**Миграция:** `supabase/migrations/20260806_activity_audit.sql` — применена владельцем к проду через Supabase MCP, живой тест (с откатом) подтвердил корректную запись и устойчивость к сбою.

**Файлы:** `App.tsx`, `services/activityAudit.ts`, `utils/activityAuditSummary.ts`, `components/Admin/ActivityAuditView.tsx`, `test/activityAudit*.test.ts(x)`, `supabase/migrations/20260806_activity_audit.sql`.

**Коммит:** `ae19281`, запушен в `main` (прод и тест — одна ветка).

---

### T70 — Поддержка нескольких мастерских (branches.is_workshop) `DONE` (2026-08-05)
**Запрос владельца:** добавить вторую мастерскую («Мастерская на Абая»), текущую переименовать в «Мастерская на Жандосова». Продавец выбирает мастерскую из списка при оформлении заказа. Адрес новой — как у «Абая 34».

**Сделано (фронт):** убран хардкод `WORKSHOP_BRANCH_ID` (~30 мест, 9 файлов) — источник истины теперь `branches.is_workshop`, читается один раз в `App.tsx` (`sidebarBranches` + `is_workshop` в select) в массив `workshopBranches: {id,name}[]`, прокидывается пропсами в `InventoryPage`/`WorkshopPage`/`HelpModal`/`AddSaleModal`.

**Исправлены скрытые баги изоляции, которые вылезли бы при двух мастерских:**
- `fetchServiceOrders` фильтровала мастера по хардкод-константе независимо от переданного `employeeBranchId` — теперь принимает явный `isMasterView` и фильтрует по реальному филиалу (каждая мастерская видит только свои заказы).
- Realtime-бейдж новых заказов в `App.tsx` был на одном общем канале для «сотрудник мастерской» — теперь канал/фильтр по конкретному `branch_id`.
- `AddSaleModal` (чекбокс «Добавить заказ в мастерскую») — добавлен `<select>` «Мастерская *» (авто-выбор, если она одна; список показывается всегда), guard в submit если не выбрана.
- Найдено ревью до коммита: `WorkshopPage` свитчер филиалов у admin показывает ВСЕ филиалы (нужно для вкладки «Журнал»), но «+ Новый заказ» не проверял, что выбранный филиал — реально мастерская → заказ мог уйти в обычный филиал и потеряться (ни один мастер бы его не увидел). Добавлена явная проверка в `handleNewOrder`.

**Проверено:** саб-агент-ревью (2 захода, нашёл и подтвердил фикс критичной проблемы выше), `tsc --noEmit` — 50 ошибок (было 55 в базе — 5 старых предсуществующих ошибок в `workshop.test.tsx` попутно исправились), `npm run build` — ок, `vitest run` — 48/48 (добавлено 5 новых тестов на мульти-мастерскую).

**БД и деплой:** миграция `branches.is_workshop` применена владельцем к проду через Supabase MCP — колонка добавлена, текущая мастерская переименована в «Мастерская на Жандосова» (id 1104bc27, is_workshop=true), создан новый филиал «Мастерская на Абая» (is_workshop=true, адрес «Абая 34 уг. Масанчи»), `get_total_unread_for_employee` обобщена под флаг. Код запушен (`a188b85` + `8b2ca29`) → автодеплой в прод и тест.

**Файлы:** `App.tsx`, `constants.ts`, `types/index.ts`, `pages/InventoryPage.tsx`, `pages/WorkshopPage.tsx`, `components/HelpModal.tsx`, `components/Inventory/AddSaleModal.tsx`, `components/Workshop/AddServiceOrderModal.tsx`, `components/Workshop/ServiceOrderCard.tsx`, `components/Workshop/WorkshopManagerView.tsx`, `services/workshop.ts`, `test/addSaleModal.prepayment.test.tsx`, `test/workshop.test.tsx`.

---

### T69 — Предоплата (долг) на обычный товар без мастерской `DONE` (2026-08-05)
**Запрос владельца:** пункт 1 из трёх задач на разбор — предоплата на товары без мастерской, т.е. частичная оплата с долгом клиента.

**Сделано:**
- `AddSaleModal.tsx`: переключатель «Полностью / Предоплата» для обычной продажи (без мастерской), тот же недеструктивный клемп (`onChange` без клемпа → `onBlur` клемп [0, сумма товаров] → красная подсказка → guard в submit), что уже используется для предоплаты мастерской/предзаказа.
- Новые аддитивные колонки `sales.debt_amount` / `debt_paid_at` / `debt_payment_method` — `sales.status` не тронут (остаётся `paid` даже при долге, чтобы не путать со смыслом `pending` = ожидание Kaspi QR).
- `settleSaleDebt()` в `inventory.ts` — погашение долга, `UPDATE ... WHERE debt_paid_at IS NULL` (атомарная защита от гонки при двойном клике/двух вкладках).
- `InventoryPage.tsx`: бейдж долга в карточке продажи и в деталях, кнопка «Погасить долг» (наличные/Kaspi), фильтр «Только с долгом».
- Касса (`cashAdmin.ts`, `CashSessionCard.tsx`, `AdminCashView.tsx`): погашение долга учитывается отдельной строкой по дню ПОГАШЕНИЯ (не по дню продажи) — тот же паттерн, что уже был для доплаты остатка мастерской (`original_prepayment`/`remaining_paid_at`).

**Найден и исправлен критичный баг до коммита (саб-агент-ревью):** первая версия `settleSaleDebt` прибавляла долг к `sales.paid_cash`/`paid_kaspi` — а касса отдельно суммирует и `paid_cash` (по дате продажи), и погашённый долг (по дате погашения). При погашении в тот же день, что и продажа, сумма в кассе задваивалась. Исправлено: погашение больше не трогает `paid_cash`/`paid_kaspi`, только `debt_paid_at`/`debt_payment_method`. Повторное ревью подтвердило устранение.

**Миграция:** `supabase/migrations/20260805_sales_debt_amount.sql` — применена владельцем к проду (`toxspgdkvxmpsvtecesy`) через Supabase MCP до коммита кода, колонки проверены на месте.

**Проверено:** 43/43 теста зелёных (+3 на клемп/долг в `AddSaleModal`, +5 на `settleSaleDebt`), `tsc` — 55 ошибок (база не выросла), `npm run build` — ок.

**Файлы:** `AddSaleModal.tsx`, `services/inventory.ts`, `types/index.ts`, `pages/InventoryPage.tsx`, `services/cashAdmin.ts`, `components/Inventory/CashSessionCard.tsx`, `components/Admin/AdminCashView.tsx`, `test/addSaleModal.prepayment.test.tsx`, `test/settleSaleDebt.test.ts`, `supabase/migrations/20260805_sales_debt_amount.sql`.

**Коммит:** `a22bc6e`, запушен в `main` (прод и тест — одна ветка).

**Не сделано в этой сессии (пункты 2 и 3 из исходного разбора вариант 2 сделан ранее как T68; пункт 3 — категории оправ в мастерской — не начат, ждёт бизнес-решения владельца).**

---

### T68 — Приёмка перемещений: кнопка «Подтвердить всё» `DONE` (2026-08-05)
**Запрос владельца:** из трёх задач на разбор (предоплата на товар без мастерской / одобрение заявок одной кнопкой / категории мастерской) выбран для реализации только пункт 2, Вариант 1 (минимальный).

**Контекст:** в `IncomingTransfersModal.tsx` (приёмка филиалом уже отправленного складом товара) каждая позиция заявки — отдельная строка `stock_movements`, отдельная карточка с кнопками «Подтвердить приёмку»/«Отклонить» по одной. Массового подтверждения не было.

**Сделано:** кнопка «Подтвердить всё (N)» сверху списка — подтверждает все позиции БЕЗ расхождения (где фактическое количество совпадает с отправленным) последовательно (не параллельно — `confirmTransfer` дважды дёргает `recalculate_stock(branch_id)` без явной блокировки, параллельные вызовы на один филиал могли бы гонять пересчёт остатков; нашёл ревьюер, поправил до коммита). Позиции с расхождением остаются на индивидуальных кнопках. Частичный сбой не маскируется под успех — сообщение «Подтверждено X из Y. Не удалось: …» с причиной по каждой упавшей позиции. Кнопки блокируются друг от друга на время выполнения (`bulkConfirming`), защита от двойного клика через `submittingRef`. Заодно убрано дублирование баннера ошибки (раньше рендерился в каждой карточке одновременно + ещё раз внизу списка — теперь один баннер сверху).

**Пункты 1 (предоплата на товар без мастерской) и 3 (категории оправ в мастерской) — не трогали**, ждут отдельного решения владельца (детали см. в разборе выше по сессии — пункт 1 требует миграции БД, пункт 3 требует бизнес-решения).

**Проверено:** саб-агент-ревью (нашёл и был исправлен риск гонки при параллельном пересчёте остатков), `tsc --noEmit` — 55/55 (не выросло), `npm run build` — ок, `vitest run` — 35/35 зелёных. Живой тест на dev-test/проде не проводился в этой сессии — просят провести владельца по сценарию: заявка на 2-3 товара → отправка со склада → «Входящие перемещения» → «Подтвердить всё» → сверить зачисление остатков, отдельно сценарий с одной ошибкой (частичный успех).

**Файлы:** `src/components/Inventory/IncomingTransfersModal.tsx`.

**Коммит:** `cb66dc4`, запушен в `main` (прод и тест — одна ветка).

---

### T67 — Новая продажа: предоплата мастерской не ограничена стоимостью услуги, итог выглядит «прибавляется без причины» `DONE` (2026-08-03)
**Жалоба менеджера (срочно, прод):** в модале «Новая продажа» с заказом в мастерскую при «Стоимость услуги»=0, «Стоимость запчастей»=0, «Тип оплаты»=Предоплата, «Сумма предоплаты»=50 000 и товарах на 65 000 — «Итого к оплате сейчас» показывало 115 000, менеджер не мог понять почему и не мог оформить предоплату.

**Причина:** `src/components/Inventory/AddSaleModal.tsx`, поле «Сумма предоплаты» — клемп в `onChange` имел лазейку: `setWorkshopPrepayment(max > 0 ? Math.min(val, max) : val)`. Когда `workshopServicePrice + workshopPartsPrice === 0`, клемп полностью отключался и позволял ввести любую сумму предоплаты, хотя по смыслу предоплата не может превышать стоимость услуги+запчастей (это уже было целью T17, но лазейка сводила проверку на нет). Дополнительно: клемп срабатывал только при вводе в само поле предоплаты — если стоимость услуги/запчастей уменьшали ПОСЛЕ того как предоплата уже введена, предоплата не пересчитывалась.

Сама формула «Итого к оплате сейчас» (`total + workshopPrepayment` для режима «Предоплата») была верной изначально — товары полностью + предоплата мастерской, без двойного учёта. Баг был именно в отсутствии ограничения на сумму предоплаты, из-за чего получались бессмысленные комбинации (предоплата 50 000 при стоимости услуги 0).

**Исправление:**
1. Убрана лазейка в `onChange` — клемп `Math.min(val, max)` теперь работает всегда, включая `max === 0` (тогда предоплата принудительно 0).
2. Добавлен `useEffect` на `[workshopServicePrice, workshopPartsPrice]`, который переклэмпит уже введённую предоплату, если стоимость услуги/запчастей уменьшили задним числом.
3. Когда `max === 0`, вместо пустой строки под полем показывается подсказка «Сначала укажите стоимость услуги и/или запчастей» вместо строки «Остаток».

**Проверено вживую на `dev-test`:** товар ₸65 000 + мастерская, все 3 режима оплаты мастерской (При получении / Предоплата / 100% сразу), скидка 10% на товар, клемп предоплаты (ввод 100 000 при максимуме 70 000 → обрезается до 70 000), реактивный переклэмп (уменьшение стоимости услуги после ввода предоплаты → предоплата уменьшается автоматически), смешанная оплата (наличные+Kaspi QR, валидация недоплаты сработала корректно). tsc (55 ошибок — база не выросла), vitest (13/13 зелёные), build — все ок.

**Не удалось проверить до конца:** реальную запись продажи в БД на `dev-test` — тестовый Supabase (`afpvhenzqtadukrmgrye`) не синхронизирован со схемой прода: в `sale_items` отсутствует колонка `list_price` (добавлена для фичи «показ original price и скидки», см. коммит `00eb21e`/`4a7e20a`). На проде колонка есть, тест — нет. Не в скоупе этой задачи, отдельная проблема теста; создал один «осиротевший» тестовый sale-ряд (₸65 000, без товаров) при отладке — не критично, тестовые данные.

**Файлы:** `src/components/Inventory/AddSaleModal.tsx`.

---

### T66 — Заявка на склад: кнопка «Отправить» на одобренной заявке ничего не делает `IN_PROGRESS` (2026-07-31)
**Жалоба:** филиал «Гум» (менеджер Дианара) создал заявку, склад одобрил (статус «Одобрена», видно количество позиций), но при нажатии зелёной кнопки «Отправить» ничего не происходит — статус не переходит в «Отправлена». У других заявок статус «Отправлена» уже стоит, т.е. раньше (или для других сотрудников/заявок) отправка отрабатывала.

**Гипотеза (не подтверждена на проде — не было доступа к БД в этой сессии):** та же ловушка PostgREST/RLS, что уже дважды всплывала на этой же таблице:
- T45 (2026-07-05): политика `admin_update_requests` на `stock_requests` разрешала UPDATE только `admin`/`branch_admin` — кладовщик с ролью `manager` не мог даже «Одобрить» (0 строк молча, без ошибки).
- T48 (2026-07-06): на `stock_request_items` вообще не было политики UPDATE — PATCH возвращал 204 (успех), но 0 строк обновлялось.

В обоих случаях `.update(...)` в `inventory.ts` проверял только `error` (который остаётся `null` при заблокированном RLS UPDATE), а не реальное число изменённых строк — из-за этого баг и выглядел как «молчаливый провал». Скорее всего сейчас аналогичная ситуация: либо политика на `stock_requests` не покрывает переход `approved → shipped` для конкретного сотрудника/роли, либо регресс той же природы.

**Что сделано (код, без доступа к прод-БД для подтверждения точной причины):**
`shipStockRequest()`, `approveStockRequest()`, `rejectStockRequest()`, `updateStockRequestItemQuantities()` в `src/services/inventory.ts` — ко всем финальным `.update(...)` добавлен `.select('id').single()` (тот же приём, что уже используется в `updateRevisionItem`): если RLS молча блокирует UPDATE (0 строк), `.single()` на пустом результате возвращает ошибку PostgREST вместо тихого успеха — пользователь теперь увидит алерт с понятным текстом («нет прав на изменение, обратитесь к администратору») вместо зависшего статуса без объяснений.

**Не проверено:**
- Не воспроизведено на реальном прод-аккаунте склада (нет учётных данных/доступа к БД в этой сессии) — не подтверждено, что причина именно в RLS, а не в чём-то ещё (например, конкретная политика могла разрешать UPDATE только когда `status = 'new'`, блокируя переход `approved → shipped`).
- Если после этого фикса пользователь при следующем клике на «Отправить» увидит алерт с ошибкой — это подтвердит гипотезу, и нужно будет посмотреть/поправить саму RLS-политику на `stock_requests` (SQL показать пользователю на проверку перед применением, миграции применяет владелец через Supabase MCP).
- tsc/vitest/build — зелёные. Живой UI-тест на dev-test не делался (изменения только в error-handling существующих функций, поведение success-path не менялось).

**Файлы:** `src/services/inventory.ts`.

---

### T65 — Печать этикетки: цифры под штрихкодом не видны + позиционирование (40×10мм, 14-значный код НКТ) `IN_PROGRESS` (2026-07-13, обновлено 2026-07-15)
**Файл:** `print_server.py` (НЕ `print_server_v2.py` — вся отладка шла именно в оригинальном файле)

**Обновление 2026-07-15 — найдена корневая причина, печать подтверждена рабочей:**
1. **X-координата цифр под штрихкодом** — баг клемпа: `final_text_x = max(0, min(text_x + SHIFT_X, W - text_w - margin_x))` сравнивал уже сдвинутую координату (`text_x+SHIFT_X`, физическая ~309) с несдвинутой границей (`W-text_w-margin_x`, ~199) — `min()` почти всегда выбирал несдвинутую, цифры печатались на ~14мм левее штрихкода. Исправлено: клемп в несдвинутой системе координат, `SHIFT_X` добавляется последним.
2. **5мм зазор между ценой и штрихкодом** — раньше цена центрировалась вплоть до линии сгиба, штрихкод оказывался вплотную. Добавлен `bc_gap` — сужена только зона центрирования цены (сам штрихкод не тронут, чтобы не испортить сканируемость).
3. **Корневая причина нестабильности вертикали (после нескольких неудачных подборов офсета +20/-20/-40/-30мм — см. историю ниже):** длина зазора между этикетками в TSPL-команде `GAP` была указана **7мм**, хотя пользователь замерил линейкой по размеченному фото — реальный зазор **2.5мм**. Принтер был настроен ждать зазор втрое длиннее физического, из-за чего датчик систематически путал границы этикеток — это и объясняло разброс результатов при подборе offset. Исправлено: `GAP 2.5 mm,0 mm` (offset сброшен на 0 — все предыдущие подобранные offset-значения калибровались против неверной базы, больше не актуальны).
4. Пользователь также откалибровал сам принтер аппаратно (кнопка FEED при включении, отпуск на красном мигании — по официальному TSC-мануалу, gap/black mark sensor calibration).
5. **Результат:** «распечаталось отлично» (фото + подтверждение пользователя). Пользователь продолжает тестировать дальше на своих сценариях, статус пока не переведён в DONE — ждём финального подтверждения после серии тестов.

**Debug-вывод (`[DEBUG] GAP ...`, `[DEBUG] barcode=...`) сознательно оставлен в коде** — пригодится, если в дальнейшем тестировании всплывёт ещё что-то. Убрать только после полного подтверждения.

**Не проверено отдельно:** жалоба «криво» (если имелась в виду не вертикальная позиция, а перекос/наклон) может быть механической — этикетка 7см шириной с хвостиком, свисающим только с одной стороны, теоретически может неровно подаваться через ролики. Программным офсетом это не лечится. Если перекос повторится — смотреть на сам механизм подачи, не в код.

**Контекст:** директор тестировал печать этикеток на ПК заказчика (там нет git-репозитория,
обновление только через перенос `TSC_PrintServer.exe` флешкой — см. ниже). Товар для теста:
"Mustang 280000", штрихкод `01506084055824` (14 цифр — ведущий 0 добавлен из-за перехода
на НКТ; печатаются и **сканируются** тоже 14-значные, это подтверждённое требование, не баг).

**Найдено и исправлено по пути (все шаги реально задеплоены заказчику, но финальный результат
на 2026-07-13 конец сессии — НЕ подтверждён):**

1. **Цена печаталась перевёрнутой/криво** — коммит `9f963fa` (2 июля, отдельная сессия) убрал
   защиту от отрицательных координат (`max(0, ...)`) при увеличении `SHIFT_Y` до 8мм, координаты
   уходили в минус (top_y=-56, p_y=-32). Вернул клемп — цена стала печататься правильно
   (подтверждено фото пользователя, "280 000" не перевёрнута).

2. **Цифры под штрихкодом не показывались вообще.** Расследование заняло несколько итераций:
   - Оказалось, этикетка **40×10мм**, не 45×10 как предполагалось изначально — все ручные
     расчёты по формулам до добавления debug-вывода были на основе неверного `W`
   - Добавлен `print(...)` debug-вывод прямо в `build_tspl()` (виден в консоли exe при печати) —
     это дало точные реальные числа вместо расчётов "на бумаге"
   - Реальные данные (первый прогон): `final_text_x=207, text_w=112, W=319` — подпись **упиралась
     ровно в правый край без запаса** (207+112=319=W). Добавлен отступ `margin_x` — теперь
     `W - text_w - margin_x`
   - Второй прогон (после отступа): та же проблема осталась. Гипотеза: белый "закрывающий" блок
     (`BITMAP ... cover_h`, добавлен ещё коммитом `598e18f` — прошивка TSC игнорирует
     `human_readable=0` в команде `BARCODE`, поэтому родной текст принтера приходится закрывать
     белым прямоугольником, а затем поверх рисовать свой мелкий шрифт) занимал Y=[39,55], а
     моя же "центрирующая" логика (не финальная — уже заменена) ставила текст на Y=51, **внутри**
     диапазона белой заливки. Похоже, эта конкретная прошивка не гарантирует порядок отрисовки
     строго по порядку TSPL-команд.
   - **Последний примененный фикс** (НЕ подтверждён): `final_text_y = min(text_y + cover_h + 2,
     H - text_h)` — текст ставится строго ПОСЛЕ белого блока (а не "по центру"), чтобы
     физически не пересекаться с ним ни при каком порядке отрисовки.

**Debug-вывод оставлен в коде намеренно** (не финальное решение, чисто диагностика) — строка вида:
```
[DEBUG] barcode="..." len=14 H=79 W=319 narrow=1 bar_h_bc=39 top_y=0 text_y=39 cover_h=16
final_text_x=... final_text_y=... text_w=112 SHIFT_X=120
```
Убрать после подтверждения финального фикса.

**Резервные копии (не в git, локально на диске разработчика):**
- `print_server_before_clamp_fix.py` — версия ДО всех сегодняшних правок (на случай отката)
- `C:\Users\indpa\Downloads\TSC_PrintServer.exe` — последняя собранная версия (с фиксом
  "текст после cover_h"), скопирована на флешку и передана на ПК заказчика на момент
  окончания сессии
- **`print_server.py` НЕ закоммичен в git** — есть незакоммиченные изменения (`git status`
  показывал `M print_server.py`), нужно закоммитить после подтверждения, что фикс работает

**Что делать в новой сессии:**
1. Спросить пользователя — подтвердился ли последний фикс (фото этикетки + `[DEBUG]` строка
   из консоли)?
2. Если ДА — убрать debug-`print()`, закоммитить `print_server.py` в git, пересобрать
   финальный `.exe` без debug-вывода, обновить статус этой задачи на `DONE`
3. Если НЕТ — свежие `[DEBUG]` числа покажут, что именно происходит; не гадать формулами,
   а читать реальные цифры из консоли принтер-сервера
4. Собранный `.exe` качественно тестировать может только пользователь физически на месте —
   Claude не имеет доступа к ПК заказчика напрямую, весь перенос идёт через флешку

**Урок сессии (см. также CLAUDE.md "Работа с задачами"):** несколько раз пробовал чинить
позиционирование "на бумаге" по формулам без реальных данных — не сработало 3 раза подряд.
Debug-вывод с реальными числами сразу показал точную причину. В следующий раз для подобных
задач — сразу добавлять диагностический вывод, не тратить итерации на ручной расчёт координат.

---

### T64 — Печать этикетки для товара без штрихкода печаталась криво `DONE` (2026-07-10, ретроактивно задокументировано 2026-07-12)
**Файлы:** `print_server.py`, `src/components/Inventory/ProductDetailModal.tsx`

**Найдено пользователем (Telegram-сессия, тестировался товар "Mustang 280000"):** этикетка
у товара без штрихкода печаталась криво/смещённо.

**Расследование (2 июля, тоже через Telegram) шло не в ту ветку кода** — правили `SHIFT_Y`
в ветке "штрихкод есть" (коммит `9f963fa`, сдвиг 8мм без ограничения координат), результат
не подтверждён, сессия ушла в переписывание `print_server_v2.py`. Реальный баг жил в
**соседней ветке "штрихкода нет"**: TSPL-рендеринг игнорировал калибровку `SHIFT_X` и жёстко
ставил текст на `x=W//2`.

**Исправление (10 июля):**
1. `print_server.py` — для товаров без штрихкода цена теперь центрируется по всей ширине
   этикетки шрифтом 3, с учётом `SHIFT_X` (было — игнорировался)
2. `ProductDetailModal.tsx` — кнопка печати этикетки добавлена в футер карточки товара;
   раньше иконка печати была только внутри строки штрихкода и **не отображалась вообще**,
   если у товара нет штрихкода — не было способа даже вызвать печать для проверки фикса

**Урок (см. также правило в CLAUDE.md "Работа с задачами"):** ни фикс от 2 июля, ни фикс
от 10 июля не попали в TASKS.md вовремя — оба нашлись только 12 июля через прямой поиск
по git log/session transcripts, инициированный пользователем. Причина находки — сообщение
из Telegram-чата ("Запушено. Перезапусти print_server.py на ПК и проверяй Mustang 280000"),
процитированное пользователем вручную.

---

### T61 — Редактирование прихода, тихое падение удаления, доступ склада `DONE` (2026-07-09)
**Файлы:** `src/services/inventory.ts`, `src/components/Inventory/AddPurchaseModal.tsx`, `src/pages/InventoryPage.tsx`

**Найдено пользователем:** кнопка «Удалить приход» в списке «Приходы» "не удаляла". Также запрошена возможность редактировать уже сохранённый приход.

**Расследование:** RLS на `purchase_orders`/`purchase_order_items` открыт, внешний ключ (`purchase_order_items → purchase_orders`) с `ON DELETE CASCADE`, триггер `trigger_delete_stock_on_receipt_delete` при удалении прихода сам чистит связанные `stock_movements` и пересчитывает остатки по всем филиалам — код удаления в БД в порядке. Реальная находка: ошибка при удалении **тихо уходила в `console.error`**, пользователю ничего не показывалось — выглядело как "ничего не происходит" независимо от причины.

**Сделано:**
1. `deletePurchaseOrder` теперь показывает `alert` с текстом ошибки при сбое; поправлен устаревший текст подтверждения (раньше писал "остатки не будут скорректированы" — неправда, триггер их корректирует).
2. Новая функция `updatePurchaseOrder()` в `inventory.ts` — редактирование уже сохранённого прихода: позиции и `stock_movements` полностью пересоздаются (не построчный diff — проще и надёжнее при произвольных изменениях состава), остатки пересчитываются через `recalculate_stock` (у `stock_movements` нет триггера на DELETE, только на INSERT, поэтому после удаления старых движений явный пересчёт обязателен). Филиал получения при редактировании зафиксирован — менять его через эту форму нельзя (риск рассинхронизации остатков между филиалами).
3. `AddPurchaseModal` получил режим редактирования (`initialData.purchaseOrderId`) — то же окно, что и создание, с предзаполненными поставщиком/датой/примечанием/позициями/филиалом.
4. Кнопка «Редактировать» (карандаш) — рядом с «Удалить» в списке приходов.
5. **Доступ:** обе кнопки были видны только `admin`/`branch_admin` (`role !== 'manager'`) — обычный менеджер склада вообще их не видел. Расширено по образцу уже существующего паттерна (`canManageRequests`/`canSeeLabelsTab` = admin ИЛИ склад): теперь `role !== 'manager' || isWarehouseBranch` — менеджер филиала «Склад» тоже получил доступ (без изменения доступа к себестоимости — то отдельное правило не трогали).

**Проверено пользователем на тесте:** работает.

---

### T60 — Сверка всех SQL-изменений сессии на проде `DONE` (2026-07-07)
**Где:** Supabase Postgres, прод `toxspgdkvxmpsvtecesy`

**Контекст:** пользователь попросил сверить, всё ли из SQL/DB-правок этой сессии реально применено на проде (не только на тесте). Прогнал прямой SQL-чек по всем пунктам:
- T52 (`cash_session_closures` + `reopen_cash_session`) — ✅ было применено
- Constraint `sales_payment_method_check` (halyk/kaspi_transfer) — ✅ было применено
- `clients.is_active` — ✅ было применено
- URL 3 push-триггеров (T59) — ✅ было применено
- **RLS `admin_update_requests`** (branch_admin только свой филиал) — ❌ **найден пропуск**: SQL был дан пользователю и накатан только на тест ("Success. No rows returned"), явного "работает, катим на прод" не прозвучало — разговор ушёл в тестирование других багов, и катка на прод была пропущена.

**Исправление:** накатан тот же SQL на прод, проверено (`qual` теперь содержит `branch_id = get_user_branch_id()`).

**Вывод на будущее:** после каждого "накатить на тест" всегда явно спрашивать/фиксировать отдельным шагом "катим то же на прод?", а не полагаться на то, что тема всплывёт снова сама по себе в разговоре.

---

### T59 — Push задач/внутреннего чата/перемещений слал запрос на СТАРЫЙ Supabase-проект `DONE` (2026-07-07)
**Где:** Supabase Postgres-функции (не код фронтенда), прод `toxspgdkvxmpsvtecesy`

**Найдено пользователем:** директор поставил задачу менеджеру Гум — push не пришёл.

**Причина:** при миграции на новый Supabase-проект (T34, 2026-06-24) три DB-триггерные функции забыли обновить — они продолжали слать `net.http_post` на **старый** проект `ygvsnecgmoiwlkzkerhu` (тот, что помечен "не использовать" в CLAUDE.md), поэтому push реально никуда не долетал с самой миграции:
- `notify_push_new_task` — push при назначении задачи
- `notify_push_internal_message` — push во внутреннем чате компании
- `notify_push_on_transfer` — push при перемещении товара на филиал

Только `notify_push_new_message` (push по входящим сообщениям клиентов, WhatsApp) была обновлена верно при миграции — поэтому именно эта категория пушей работала (подтверждена в T47), а остальные три — нет, никто не тестировал их отдельно.

**Важно:** push по заявкам на склад (T39/T45/T47) шлётся не триггером БД, а напрямую из фронтенда (`notifyBranch()` в `services/inventory.ts`/`services/workshop.ts` через `supabase.functions.invoke('send-push', ...)`) — использует текущий клиентский конфиг, поэтому не затронут этим багом и работал всё время корректно.

**Исправление:** во всех трёх функциях заменён URL с `ygvsnecgmoiwlkzkerhu` на `toxspgdkvxmpsvtecesy`, остальная логика не менялась. Применено на прод, проверено (`ilike '%toxspgdkvxmpsvtecesy%'` = true во всех четырёх функциях, старого URL не осталось).

**Не проверено на тесте** — тестовый проект (`afpvhenzqtadukrmgrye`) отдельный, недоступен через MCP; если там такая же копия схемы, нужен тот же фикс, но с URL теста, а не прода.

---

### T58 — Кнопка «Создать контакт», удаление контакта, поиск клиентов в чатах `DONE` (2026-07-07)
**Файлы:** `src/components/Chat/ChatList.tsx`, `src/components/Chat/NewChatModal.tsx`, `src/components/Chat/ChatInfoPanel.tsx`, `src/services/chats.ts`, миграция `clients.is_active`

**Что сделано:**
1. Мелкая карандаш-иконка в шапке «Чаты» заменена на видную кнопку «Создать контакт».
2. Contact Picker API («Выбрать из контактов», работал только на Android) убран полностью — на iPhone он не поддерживался и ничего не делал, оставлен единый флоу поиска/создания через ручной ввод.
3. Добавлена колонка `clients.is_active` (миграция, применена на проде и на тесте) — soft-delete контактов. `getChats()`/`searchClientsForChat()`/поиск клиента в `AddSaleModal` теперь исключают неактивных клиентов. Функция `deleteClient()`.
4. Кнопка «Удалить контакт» в `ChatInfoPanel` (секция «Действия») — **только admin/branch_admin**, обычный менеджер видит только «Архивировать чат» (осознанное решение после обсуждения — сначала открыли всем ролям, потом откатили).
5. Верхний поиск на вкладке «Чаты» — раньше фильтровал только уже существующие чаты (если чатов 0 — не мог ничего подсказать). Добавлены подсказки по клиентам без чата (`searchClientsForChat`, отфильтровано по `!chatId`) с кнопкой «Написать» — тап открывает/создаёт чат, как в WhatsApp.
6. Бейдж «статус моей заявки изменился» (T56) — исправлен: сама вкладка «История заявок» не показывала никакого индикатора для менеджера (только кнопка нижней навигации), из-за чего непонятно куда смотреть после захода в «Склад». Добавлен тот же синий счётчик на саму вкладку.

**Побочная находка при тестировании:** менеджер увидел сразу 6 непрочитанных статусов при первом входе после T56 — ожидаемо (backlog старых изменений статуса без механизма "просмотрено" до этой сессии), не баг.

---

### T57 — Имя клиента при создании нового чата на iPhone `DONE` (2026-07-07)
**Файл:** `src/components/Chat/NewChatModal.tsx`

**Найдено пользователем:** на iPhone кнопка «Выбрать из контактов» не поддерживается (Contact Picker API — только Android Chrome), поэтому создание чата шло через ручной ввод номера → «Создать новый чат». Но поля для имени не было нигде — чат создавался только с номером телефона, без возможности сразу подписать, кто это.

**Причина:** `createClientAndChat(phone, name, ...)` в `services/chats.ts` уже принимал необязательное имя, но UI никогда не отображал поле ввода для него в ручном флоу — `contactName` заполнялся только результатом Contact Picker API (Android), для остальных случаев оставался `undefined`.

**Исправление:** добавлено поле «Имя клиента (необязательно)» перед кнопкой «Создать новый чат» — в обоих режимах модала (обычном и iOS tel-режиме), привязано к тому же состоянию `contactName`.

---

### T56 — Бейдж «статус моей заявки изменился» для менеджера филиала `DONE` (2026-07-06)
**Файлы:** `src/App.tsx`, `src/pages/InventoryPage.tsx`

**Контекст:** после T55 (история заявок для менеджера, read-only) выяснилось при тестировании: когда склад одобряет/отклоняет/отправляет заявку менеджера, приходит push-уведомление на компьютер менеджера, но никакого следа в самом приложении нет — негде свериться, кроме как случайно зайти во вкладку и проверить вручную.

**Что сделано:**
- Новый бейдж `myRequestStatusBadge` (App.tsx) — для менеджеров обычных филиалов (не admin, не склад, не мастерская): считает свои же заявки, у которых текущий `status` отличается от последнего просмотренного (localStorage `stock_request_seen_status`, ключ requestId → status, тот же паттерн, что уже использовался для `workshop_read_ids`/`inventory_workshop_read_ids`). `status = 'new'` не считается — это заявка, только что созданная самим менеджером, ему нечего "узнавать".
- Подписка на realtime (`postgres_changes` по `stock_requests` своего филиала) + добавлен в общий `totalBadge` (иконка PWA).
- Отображается синим счётчиком на кнопке «Склад» у менеджера (desktop и мобильный bottom bar), в приоритете перед красной точкой `hasPendingTransfers`.
- Гасится автоматически, когда менеджер открывает вкладку «История заявок» (InventoryPage: useEffect записывает текущие статусы всех загруженных заявок в localStorage и шлёт `stock-request-status-read` событие) — это осознанно НЕ нарушает антипаттерн "просмотр ≠ разрешено" из более ранних фиксов, так как здесь никакого действия от менеджера не требуется, только уведомление о факте изменения.

**Побочная находка при обсуждении (не баг, подтверждено пользователем):** складской сотрудник получил 2 одинаковых push на одно событие — оказалось, под тестовым складским аккаунтом есть 2 активные push-подписки (`web.push.apple.com` и `fcm.googleapis.com`), потому что заходили и с телефона, и с компьютера — ожидаемое поведение multi-device push (T32), не трогали.

---

### T55 — Бейдж «Склад» для admin, разбивка Halyk/Kaspi-перевод в «Касса», история заявок для менеджера `DONE` (2026-07-06)
**Файлы:** `src/App.tsx`, `src/components/Admin/AdminCashView.tsx`, `src/pages/InventoryPage.tsx`

**Найдено пользователем при тестировании на телефоне под admin:** бейдж на иконке PWA показывал непрочитанное, но внутри приложения не было ни одного визуального указателя, где искать.

1. **Бейдж «Склад» отсутствовал у admin.** Кнопка «Склад» в навигации admin (десктопная сетка иконок и мобильная верхняя сетка) вообще не отображала бейджи `stockRequestBadge`/`pendingTransfersCount`, хотя оба входят в `totalBadge` (управляет иконкой PWA) — у менеджера тот же индикатор уже работал. Добавлены те же бейджи (синий счётчик заявок / красная точка перемещений), что у менеджерской кнопки «Склад».
2. **`InventoryPage` для admin рендерился без `onPendingTransfersChange`** — счётчик входящих перемещений для admin никогда не обновлялся (у менеджера проп передавался, у admin — нет). Добавлен проп.
3. **Разбивка Halyk/Kaspi-перевод в списке филиалов «Касса».** `AdminCashView` → `ListScreen` (сводная карточка «Все филиалы» и карточка каждого филиала) показывала только Нал/Kaspi/Итого — суммы Halyk и Kaspi-перевод были только внутри `systemTotal`, без отдельной строки (в отличие от `CashSessionCard`, где разбивка уже была сделана в T53). Добавлены отдельные строки для обеих карточек, по образцу `CashSessionCard`.
4. **История заявок недоступна менеджеру филиала.** Вкладка «Заявки» была видна только `admin`/складу (`canSeeRequestsTab = role === 'admin' || isWarehouseBranch`) — обычный менеджер, отправивший заявку, не мог посмотреть её статус (сам нашёл при проверке через менеджера Гум). Данные уже были готовы (`getStockRequests(branchId)` фильтрует по своему филиалу), не хватало только доступа к вкладке. Открыта в режиме только для чтения: `canSeeRequestsTab` теперь включает и обычных менеджеров (тех, кто может отправлять заявки), а кнопки одобрить/отклонить/отправить и редактирование количества остались только у `canManageRequests` (admin/склад).

**Побочная находка (не чинил, отдельный риск на БД):** RLS-политика `admin_update_requests` на `stock_requests` разрешает UPDATE любому `branch_admin` без проверки совпадения филиала — теоретически branch_admin одного филиала может через API одобрить/отклонить заявку другого филиала. Обсудить и почистить отдельно.

**Проверено пользователем:** все три пункта подтверждены на тесте (компьютер + телефон).

**Уточнение после проверки:** менеджеры видели лейбл «Заявки» (тот же, что у admin/склада) и путались — казалось, что там нужно самим создавать заявки, хотя это только просмотр статуса. Для `canSeeRequestsTab && !canManageRequests` лейбл вкладки и заголовок внутри неё переименованы в «История заявок» / «История заявок на склад».

---

### T54 — Смешанная оплата: KaspiQRModal показывал полный итог вместо остатка `DONE` (2026-07-06)
**Файл:** `src/components/Inventory/AddSaleModal.tsx`

**Найдено пользователем при тестировании constraint-фикса (halyk/kaspi_transfer) на тестовом окружении:** при `paymentMethod === 'mixed'` с несколькими способами (напр. наличные 10к + Halyk 15к + Kaspi-перевод 5к + Kaspi QR 20к, итого 50к) — модал Kaspi QR показывал сумму к оплате 50 000 ₸ вместо реальных 20 000 ₸.

**Причина:** `<KaspiQRModal amount={totalNow} .../>` — хардкод общего итога продажи, не учитывал что для mixed нужна только часть, приходящаяся на kaspi_qr (`kaspiAmount`, уже правильно считался и сохранялся в БД как `paid_kaspi`, но не передавался в UI-модал).

**Исправление:** добавлено состояние `kaspiQrAmount`, устанавливается вместе с `tempSaleId`/`setShowKaspiQR(true)` фактическим `kaspiAmount`; модал теперь показывает `amount={kaspiQrAmount}`.

**Проверено пользователем на тесте:** смешанная оплата (cash+halyk+kaspi_transfer+kaspi_qr) работает корректно, Kaspi QR показывает верный остаток.

---

**2026-07-06, конец дня:** T52-миграция и constraint-фикс (halyk/kaspi_transfer) накатаны на прод и подтверждены, все 7 коммитов дня запушены в main (прод+тест). Дальше — обычный TODO (T23), если не будет других указаний.

---

## 🔴 КРИТИЧНО

### T45 — Баги в заявках на склад (продолжение T39) `DONE` (2026-07-05)
**Контекст:** при реальном тестировании T39 нашлись баги — все исправлены и проверены в тестовом окружении, затем применены в проде.

**Что сделано:**
1. RLS-политика `admin_update_requests` на `stock_requests` разрешала UPDATE только `admin`/`branch_admin` — реальный кладовщик с ролью `manager` не мог "Одобрить" (0 строк молча, без ошибки). Политика заменена, теперь учитывает сотрудников филиала "Склад".
2. Push-уведомления — не доходили из-за отдельного бага (см. T47), не из-за RLS.
3. Поле количества в заявке — фикс подтверждён рабочим.

**Файлы:** `src/services/inventory.ts`, `src/pages/InventoryPage.tsx`, миграция RLS на проде применена.

---

### T47 — Push-уведомления и бейджи: полная переработка `DONE` (2026-07-05)
**Контекст:** обнаружено и исправлено при тестировании T45 в специально созданном тестовом окружении (см. [PROJECT_MEMORY.md](PROJECT_MEMORY.md) — тестовый Supabase/Vercel).

**Найденные и исправленные баги:**
1. `push_subscriptions_employee_id_key` — UNIQUE constraint был на `employee_id` **одном**, а не на паре `(employee_id, endpoint)` — сотрудник физически не мог иметь больше одной подписки (второе устройство тихо не сохранялось). Заменено на составной constraint.
2. `usePushNotifications.ts` — insert/update подписки не проверял `{error}`, поэтому сбой (в т.ч. из-за бага #1) проходил незаметно. Переведено на `upsert(onConflict: 'employee_id,endpoint')` с проверкой ошибки.
3. `main.tsx` — `navigator.clearAppBadge()` вызывался при **любом** разворачивании приложения, независимо от реального непрочитанного — бейдж на иконке PWA пропадал без причины. Убран, бейдж теперь считается только от реального состояния (`totalBadge` в App.tsx).
4. `InventoryPage.tsx` — бейдж входящих перемещений (`hasUnreadTransfers`) был завязан на "последний просмотр вкладки", а не на реальное состояние — открыл вкладку не подтвердив перемещение → бейдж исчезал. Теперь строго `incomingTransfers.length > 0`.
5. App.tsx имел **отдельный дублирующий** поллер для того же перемещения с той же проблемой (localStorage `lastViewedMovements`) — убран, заменён на прямой count через `onPendingTransfersChange`.
6. `get_total_unread_for_employee` (SQL, используется для push `badge_count`, когда не передан явно) — считал **только** непрочитанные во внутреннем чате компании, игнорируя чаты с клиентами, заявки на склад, перемещения, заказы мастерской. Переписана функция — теперь учитывает всё то же самое, что и `totalBadge` на фронте.
7. `loadStockRequests()` грузился только при клике на вкладку "Заявки" — бейдж на самой вкладке не мог появиться при свежем заходе. Теперь грузится сразу при открытии Склада.
8. Desktop-панель иконок (менеджер и admin) — кнопки растягивались криво (21×32px вместо квадрата) из-за общего `grid-cols-5` контейнера. Зафиксирован размер `w-9 h-9` на каждой кнопке.
9. Иконки на десктопе увеличены (`w-3.5`→`w-5`), у категорий бейджей теперь разные цвета ("цветовой путь"): 🔵 заявки, 🔴 перемещения, 🟢 чаты, 🟣 задачи, 🟠 магазин/мастерская.

**Подтверждено пользователем:** push доходит даже при полностью закрытом (не свёрнутом) приложении на iPhone — так и должно быть, Service Worker не требует открытой страницы.

**Известная особенность iOS (не баг):** если Notification permission на iPhone внутри PWA случайно стал `denied` — системный тумблер "Допуск уведомлений" в Настройках это **не сбрасывает** (десинхронизация iOS между системным уровнем и уровнем WebKit). Единственный способ восстановить: удалить PWA с экрана «Домой» → Настройки → Safari → Дополнения → Данные сайтов → удалить сайт → добавить заново из Safari.

**Файлы:** `src/hooks/usePushNotifications.ts`, `src/main.tsx`, `src/App.tsx`, `src/pages/InventoryPage.tsx`, SQL-миграции (constraint + RPC) применены и в тесте, и в проде.

---

### T01 — handleRefund: ошибка Supabase игнорируется `DONE`
**Файл:** `src/components/Workshop/PendingPaymentsView.tsx` функция `handleRefund`
**Проблема:** `await supabase.from(...).update(...)` — `{error}` не деструктурируется. Если обновление провалилось — UI показывает успех, в БД ничего не изменилось. Клиент не получает предоплату, менеджер думает что вернул.
**Исправление:** деструктурировать `{ error }`, при ошибке — показать сообщение и НЕ убирать заказ из списка.

---

### T02 — createReturn: ошибки обновления склада игнорируются `DONE`
**Файл:** `src/services/inventory.ts` функция `createReturn`, строки ~483, 488, 509
**Проблема:** `if (stockUpdErr) console.error(...)` — функция продолжает работу даже при ошибке обновления stock. Возврат записывается, но товар не возвращается на склад.
**Исправление:** при ошибке обновления stock — бросать исключение (`throw`), прекращать выполнение.

---

### T03 — createSale: нет отката при ошибке stock_movements `DONE`
**Файл:** `src/services/inventory.ts` функция `createSale`, строки ~400-422
**Проблема:** Шаги: создать sale → создать sale_items → создать stock_movements. Если шаг 3 упадёт — продажа создана, товар добавлен, но сток не уменьшен.
**Исправление:** если `movError` — удалить sale_items и sale (откат вручную), бросить ошибку.

---

### T04 — completeRevision: не вызывает recalculate_stock `DONE`
**Файл:** `src/services/inventory.ts` функция `completeRevision`
**Проблема:** создаёт `revision_adjust` движения но не вызывает `recalculate_stock`. После ревизии таблица `stock` не обновляется (deleteRevision, createWriteoff, confirmTransfer — все вызывают).
**Исправление:** добавить `await supabase.rpc('recalculate_stock', { p_branch_id: revision.branch_id })` после завершения ревизии.
**Примечание:** сначала проверить через Supabase есть ли автотриггер на `revision_adjust` движения.

---

### T05 — Race condition при продаже/списании/перемещении `DONE`
**Файлы:** `src/services/inventory.ts` функции `createTransfer`, `createWriteoff`
**Проблема:** паттерн read→check→update не атомарен. Два менеджера одновременно могут продать один последний товар.
**Исправление:** добавить DB-level проверку через RPC или использовать `UPDATE stock SET quantity = quantity - N WHERE quantity >= N RETURNING quantity` и проверять результат.
**Примечание:** требует DB-решения через Supabase MCP.

---

## 🟡 СРЕДНЕ

### T06 — AddPurchaseModal: тихий скан несуществующего штрихкода `DONE`
**Файл:** `src/components/Inventory/AddPurchaseModal.tsx` функция `handleBarcodeDetected`
**Проблема:** `catch { /* не найден */ }` — при несуществующем штрихкоде ничего не происходит, нет сообщения.
**Исправление:** показать alert или toast «Товар не найден» при null/ошибке.

---

### T07 — AddSaleModal: поиск клиента по всем филиалам `DONE`
**Файл:** `src/components/Inventory/AddSaleModal.tsx` функция `searchClientsByName`
**Проблема:** автокомплит при вводе имени ищет клиентов по всей базе без фильтра по `branchId`.
**Исправление:** добавить `.eq('branch_id', branchId)` в запрос `searchClientsByName`.

---

### T08 — CashSessionCard: нет сообщения об ошибке при закрытии кассы `DONE`
**Файл:** `src/components/Inventory/CashSessionCard.tsx` функция `handleClose`
**Проблема:** если `close_cash_session` RPC вернул error — ничего не происходит, пользователь не видит причины.
**Исправление:** при `error` — показать `alert(error.message)` или inline-ошибку в модале.

---

### T09 — AddSaleModal: автоперезапись поля «Получено наличными» `DONE`
**Файл:** `src/components/Inventory/AddSaleModal.tsx` useEffect строки ~201-203
**Проблема:** при каждом изменении товаров поле `paidCash` сбрасывается к `totalNow`. Менеджер ввёл сумму → добавил товар → значение перетёрлось.
**Исправление:** автозаполнять `paidCash` только если поле пустое (`if (!paidCash)`).

---

### T10 — AddSaleModal: нет проверки остатка в момент submit `DONE`
**Файл:** `src/components/Inventory/AddSaleModal.tsx` функция `handleSubmit`
**Проблема:** список товаров загружается при открытии модала. Если другой менеджер продал последний товар пока форма открыта — submit пройдёт и stock уйдёт в минус.
**Исправление:** перед `createSale` — запросить актуальный `stock.quantity` для каждого товара и проверить `item.quantity <= stock_qty`.

---

### T11 — PendingPaymentsView: Realtime без фильтра по филиалу `DONE`
**Файл:** `src/components/Workshop/PendingPaymentsView.tsx` useEffect Realtime
**Проблема:** `{ event: '*', schema: 'public', table: 'service_orders' }` без фильтра — любое изменение в любом филиале вызывает `loadAll()`.
**Исправление:** добавить `filter: \`created_branch_id=eq.${branchId}\`` в Realtime подписку.

---

### T12 — ReturnModal: неверный лейбл суммы возврата мастерской `DONE`
**Файл:** `src/components/Inventory/ReturnModal.tsx` строка ~429
**Проблема:** написано «Возврат предоплаты мастерской» но сумма включает полную оплату (предоплата + доплата).
**Исправление:** заменить текст на «Возврат оплаты мастерской».

---

### T13 — WORKSHOP_BRANCH_ID хардкод в 4+ файлах `DONE`
**Файлы:** `AddSaleModal.tsx:12`, `services/workshop.ts:4`, `WorkshopPage.tsx:10`, `InventoryPage.tsx:279`
**Проблема:** ID захардкожен в 4 местах. Изменение в БД → нужно менять в 4 файлах.
**Исправление:** вынести в `src/constants.ts` и импортировать везде.

---

### T14 — WorkshopPage: ADMIN_BRANCHES хардкод `DONE`
**Файл:** `src/pages/WorkshopPage.tsx` строки 24-30
**Проблема:** имена и ID филиалов прошиты в коде вместо загрузки из БД.
**Исправление:** загружать список филиалов из `supabase.from('branches').select('id, name')`.

---

### T15 — ExpensesTab: удаление расхода без подтверждения `DONE`
**Файл:** `src/components/Inventory/ExpensesTab.tsx` функция `handleDelete`
**Проблема:** кнопка удаляет расход немедленно без диалога подтверждения.
**Исправление:** добавить `if (!confirm('Удалить расход?')) return;` перед `deleteExpense`.

---

### T16 — inventory.ts: reference_type неверный в возвратах `DONE`
**Файл:** `src/services/inventory.ts` функция `createReturn` строка ~502
**Проблема:** `reference_type: 'return'` при `reference_id: saleId` — логически неверно, reference_id содержит ID продажи.
**Исправление:** заменить на `reference_type: 'sale'`.

---

### T17 — AddSaleModal: предоплата мастерской без валидации `DONE`
**Файл:** `src/components/Inventory/AddSaleModal.tsx` секция мастерской
**Проблема:** `workshopPrepayment` можно ввести больше суммы услуги. Нет проверки.
**Исправление:** добавить валидацию `workshopPrepayment <= workshopServicePrice + workshopPartsPrice` и показывать ошибку.

---

## 🔵 ВЫПОЛНЕНО В ТЕКУЩЕЙ СЕССИИ

### T30 — Заявки на склад (stock requests) `DONE`
**Файлы:** `src/components/Inventory/StockRequestModal.tsx` (новый), `src/pages/InventoryPage.tsx`, `src/services/inventory.ts`, `src/types/index.ts`
**Что сделано:**
- Новая вкладка «Заявки» в InventoryPage (видна менеджерам-не-складу и сотрудникам склада)
- StockRequestModal: список всех товаров с остатком на складе, поиск, +/−, заметки, отправка
- Кнопка «Заявка на склад» в шапке InventoryPage для не-складских менеджеров
- Синий бейдж на вкладке «Заявки» = количество новых (status=pending)
- Одобрение: pre-validation наличия остатка по каждой позиции → `deduct_stock_atomic` RPC → статус → зачисление на филиал
- Отклонение: поле причины, статус rejected

---

### T31 — QA аудит: 16 багов исправлено `DONE`
**Файлы:** множественные (см. ниже)
**Что сделано:**
- Проведён полный аудит приложения: логика, математика, Realtime, push, AudioContext, UI
- Исправлены 16 багов:
  1. AddSaleModal: валидация mixed-оплаты перед созданием продажи
  2. AddSaleModal: `amount={totalNow}` в KaspiQRModal (был total — устаревшая сумма)
  3. AddSaleModal: guard от двойного submit через `isSubmittingRef`
  4. AddSaleModal: rollback stock через `recalculate_stock` при отмене Kaspi
  5. inventory.ts/approveStockRequest: pre-validation остатков до начала переводов
  6. ReturnModal: `workshopPaidAmount` с явной проверкой null (не `??`) для `original_prepayment=0`
  7. ReturnModal: ~35 дублирующихся атрибутов `className` объединены
  8. inventory.ts/createRevision: guard от создания второй активной ревизии
  9. KaspiQRModal: разделены два useEffect — countdown и onCancel
  10. InventoryPage: `audioCtxRef = useRef` на уровне компонента (не в useEffect)
  11. inventory.ts/completeRevision: фильтр `difference != null && !== 0` для stock_movements
  12. App.tsx/hasPendingTransfers: clearInterval в cleanup (не было утечки таймера)
  13. App.tsx: guard `empId` в setTimeout перед `loadInternalUnread`
  14. usePushNotifications: upsert по `employee_id + endpoint` для multi-device
  15. AddSaleModal: сброс `paidCash/paidKaspi` при смене способа оплаты
  16. App.tsx: звук только при `visibilityState === 'hidden'`

---

### T32 — Multi-device push: endpoint column `DONE`
**Файлы:** `src/hooks/usePushNotifications.ts`, migration `add_endpoint_to_push_subscriptions`
**Что сделано:**
- Добавлена колонка `endpoint text` в таблицу `push_subscriptions` через Supabase MCP
- `usePushNotifications` перезаписан: upsert по `employee_id + endpoint`
- Каждое устройство хранит свою подписку → уведомления приходят на все устройства

---

## 🟢 МЕЛКО

### T18 — Непоследовательный формат денежных сумм `DONE`
**Файлы:** `AddSaleModal.tsx`, `ReturnModal.tsx`, `CashSessionCard.tsx` и другие
**Проблема:** в разных местах разный формат: `₸10000`, `₸10,000`, `10 000 ₸`.
**Исправление:** создана утилита `formatMoney(n)` в `src/utils/formatters.ts` — возвращает `10 000 ₸`. Заменять постепенно при редактировании файлов.

---

### T19 — ReturnModal: тёмная тема в светлом приложении `DONE`
**Файл:** `src/components/Inventory/ReturnModal.tsx`
**Проблема:** ReturnModal использует тёмную WhatsApp-тему (#111b21) в то время как весь inventory — светлый.
**Исправление:** переверстать ReturnModal в светлой теме как у остальных модалов.

---

### T20 — createPurchaseOrder: console.error в production коде `DONE`
**Файл:** `src/services/inventory.ts` функция `createPurchaseOrder` строки ~243, 251, 272
**Проблема:** три `console.error` с `JSON.stringify` — debug-логи в production.
**Исправление:** удалить все три `console.error` (throw после них уже есть).

---

### T21 — AddSaleModal: свайп-закрытие не защищён от открытых дропдаунов `DONE`
**Файл:** `src/components/Inventory/AddSaleModal.tsx` useEffect свайп строки ~158-170
**Проблема:** нет проверки на открытые дропдауны — свайп вправо при открытом списке клиентов закрывает весь модал.
**Исправление:** добавить проверку `if (document.querySelector('[data-dropdown="true"]')) return;` в обработчик свайпа и добавить `data-dropdown="true"` на дропдауны.

---

### T22 — InventoryPage: any[] типизация для transfers `DONE`
**Файл:** `src/pages/InventoryPage.tsx` строки ~176-178
**Проблема:** `useState<any[]>([])` для `incomingTransfers` и `completedTransfers`.
**Исправление:** создать или использовать существующий тип `StockMovement` для типизации.

---

---

## 🟡 НОВЫЕ ЗАДАЧИ

### T24 — Встроенная справка по ролям `DONE`
**Файлы:** `src/components/HelpModal.tsx`, `src/App.tsx`
**Что сделано:**
- Кнопка «?» (HelpCircle) в шапке рядом с выходом — видна всем ролям
- HelpModal: полноэкранный модал с горизонтальным скроллом вкладок
- Контент разделён по ролям: менеджер / мастер (workshop branch) / руководитель / admin
- Модал открывается сразу на разделе текущей страницы (getCurrentHelpSection)

---

### T25 — Новый чат из поиска контактов `DONE`
**Файлы:** `src/services/chats.ts`, `src/components/Chat/NewChatModal.tsx`, `src/components/Chat/ChatList.tsx`
**Что сделано:**
- Починен поиск чатов: был сломан синтаксис PostgREST для related-таблиц, заменён на двухшаговый запрос через `clients`
- Кнопка-карандаш в шапке ChatList → открывает NewChatModal
- NewChatModal: поиск по имени/телефону, открытие существующего чата или создание нового
- Если клиент не найден и введён номер (≥10 цифр) → создаёт нового клиента + чат

---

### T26 — Contact Picker API в новом чате `DONE`
**Файл:** `src/components/Chat/NewChatModal.tsx`
**Что сделано:**
- Кнопка «Выбрать из контактов» (BookUser-иконка) в списке — видна только если браузер поддерживает Contact Picker API (Android Chrome)
- При нажатии открывается нативный выбор контакта с телефона
- Телефон контакта подставляется в поле поиска, имя сохраняется в `contactName`
- При создании нового клиента (`createClientAndChat`) имя из контакта передаётся автоматически
- Кнопка X в поиске очищает также и `contactName`
- Подсказка в пустом состоянии упоминает выбор из контактов если API доступен

---

### T27 — Филиал «Склад» в форме регистрации `DONE`
**Файл:** `src/components/Auth/SignupForm.tsx`
**Что сделано:**
- Добавлен филиал «Склад» в выпадающий список филиалов при регистрации нового сотрудника

---

### T28 — Кнопка добавления товара только для admin и склад-менеджера `DONE`
**Файл:** `src/pages/InventoryPage.tsx`
**Что сделано:**
- Кнопка «Добавить товар» скрыта от обычных менеджеров
- Доступна только admin и менеджерам филиала «Склад»

---

### T29 — Название филиала рядом с ролью в шапке `DONE`
**Файл:** `src/App.tsx`
**Что сделано:**
- В шапке приложения рядом с ролью сотрудника отображается название его филиала

---

### T33 — Очистка тестовых данных БД перед передачей заказчику `DONE`
**Где:** Supabase MCP (операции напрямую в БД, не код)
**Что сделано:**
- Удалены все транзакционные тестовые данные: продажи (7), позиции (7), движения склада (5), остатки (1), кассовые сессии (2), клиенты (1)
- Удалены все чаты и сообщения (chats, internal_chats, internal_messages)
- Удалён тестовый поставщик "ТОО «Оптик Дистрибьюшн»"
- Удалён тестовый товар "ТЕСТ — Оправа тестовая"
- Очищены push_subscriptions (7 старых токенов)
- Удалены тестовые сотрудники: "Test 1", "Тест Менеджер"
- Удалены 8 тестовых auth.users (*.newline.test, manager2/3@test.com)
- **База готова к передаче заказчику**

---

### T34 — Миграция на новый Supabase-проект + WhatsApp инфраструктура `DONE`
**Где:** Supabase MCP, n8n, PowerShell, Edge Functions
**Что сделано:**
- Новый Supabase-проект `toxspgdkvxmpsvtecesy` (аккаунт stanislavpak69020@gmail.com) — работает как основной
- Включён `pg_net`, обновлена функция `notify_push_new_message` на новый URL, добавлен `SERVICE_ROLE_KEY` в vault
- Задеплоена Edge Function `send-push` на новый проект, выставлены VAPID-секреты
- Обновлён `.env.local` — новый URL и anon key
- n8n outgoing workflow — URL обновлён на новый проект
- `SUPABASE_ACCESS_TOKEN` сохранён как постоянная переменная окружения Windows → `npx supabase functions deploy` работает без ручного ввода токена
- WhatsApp-цепочка проверена end-to-end: входящие ✅, исходящие ✅, push-уведомления ✅

---

### T44 — NKT barcode lookup: автоопределение товара по штрихкоду `DONE` (2026-06-29)
**Файлы:** `supabase/functions/nkt-lookup/`, `src/components/Inventory/EditProductModal.tsx`, `src/components/Inventory/AddProductModal.tsx`
**Что сделано:**
- Edge Function `nkt-lookup` — запрос в реестр товаров Казахстана (НКТ/GTIN) по штрихкоду
- EditProductModal: автопроверка при открытии — если штрихкод есть, подтягивает данные товара из НКТ
- AddProductModal: ручной запуск NKT-поиска при вводе штрихкода
- Исправлено отображение GTIN вместо NTIN в результатах поиска

---

### T39 — Двухэтапный запрос товара со склада + push-уведомления `DONE` (2026-07-02)
**Файлы:** `src/components/Inventory/StockRequestModal.tsx`, `src/pages/InventoryPage.tsx`, `src/services/inventory.ts`
**Что сделано:**
- Двухэтапный workflow: менеджер создаёт заявку → кладовщик одобряет/отклоняет
- Push-уведомление кладовщику при новой заявке, менеджеру при одобрении/отклонении
- Сканер штрихкода в StockRequestModal — быстрый поиск товара по коду

---

### T40 — Восстановление пароля (Forgot / Reset Password) `DONE` (2026-07-02)
**Файлы:** `src/components/Auth/`, App.tsx
**Что сделано:**
- Экран «Забыли пароль?» с отправкой письма через Supabase Auth
- Экран сброса пароля по ссылке из письма
- Показ/скрытие пароля в форме регистрации (иконка-глаз)

---

### T41 — Позиционирование штрихкода на этикетках TSC TE200 `DONE` (2026-07-02)
**Файлы:** `print_server_v2.py`, `src/components/Inventory/PrintLabelModal.tsx`
**Что сделано:**
- Серия калибровочных правок для узких этикеток 40×10 мм: раздельные смещения для EAN-13 и CODE128
- Исправлено позиционирование цены и штрихкода по X и Y для обоих типов
- `print_server_v2.py` — переписан print server с чистой центровкой для узких этикеток
- Уменьшена задержка между попытками сканирования с 500 мс до 100 мс
- Лимит количества печати поднят обратно до 999

---

### T42 — Поиск в истории печати этикеток `DONE` (2026-07-02)
**Файл:** `src/pages/InventoryPage.tsx` (вкладка Этикетки)
**Что сделано:**
- Поле поиска в секции «История печати» на вкладке Этикетки
- Фильтрация по названию товара и штрихкоду

---

### T43 — ScrollToTopButton + динамические филиалы в SignupForm `DONE` (2026-07-01)
**Файлы:** `src/components/ScrollToTopButton.tsx`, `src/components/Auth/SignupForm.tsx`
**Что сделано:**
- Плавающая кнопка «наверх» появляется при прокрутке страницы вниз
- Филиалы в форме регистрации загружаются динамически из БД (не хардкод)

---

### T35 — Фильтр по категориям в выборе товаров `DONE` (2026-07-03)
**Файлы:** `src/pages/InventoryPage.tsx`, `src/components/Inventory/AddSaleModal.tsx`, `src/components/Inventory/AddPurchaseModal.tsx`
**Что сделано:**
- Чипы-фильтры по категориям над списком товаров в трёх местах: вкладка «Товары», модал продажи, модал прихода
- Фильтр без лишних запросов — категории выводятся из уже загруженного списка товаров
- Свайп вправо не срабатывает при горизонтальном скролле чипов: `data-no-swipe="true"` + проверка в обработчике

---

### T36 — Улучшение UI групп товаров `DONE` (2026-07-03)
**Файлы:** `src/pages/InventoryPage.tsx`, `src/components/Inventory/EditProductModal.tsx`
**Что сделано:**
- Заголовок группы: название на первой строке полностью, кол-во/наличие — на второй (без truncation)
- Строки товаров внутри группы: двухстрочный layout — название товара целиком + штрихкод/цена на второй строке
- Кнопки переименования группы (✓ / ✕): `min-w-0` на input + `flex gap-3 flex-shrink-0` — обе кнопки видны
- EditProductModal: поле «Группа» с кастомным dropdown (список существующих групп + ввод новой)

---

### T37 — Параметры линз и оправы в EditProductModal `DONE` (2026-07-03)
**Файл:** `src/components/Inventory/EditProductModal.tsx`
**Что сделано:**
- Секция «Параметры линз» (синяя): sphere, cylinder, axis, diameter, base_curve — появляется при категории lens/contact
- Секция «Параметры оправы» (фиолетовая): color, size, material, gender — появляется при категории frame/glass/sun
- Поля предзаполнены из существующих `product.attributes`
- При сохранении все атрибуты записываются обратно в БД

---

### T38 — Graphify knowledge graph `DONE` (2026-07-03)
**Где:** `graphify-out/` (локально, исключён из git)
**Что сделано:**
- Установлен `graphifyy` (pip), построен полный граф проекта: 852 узла, 1 388 рёбер, 78 сообществ
- `graphify-out/` добавлен в `.gitignore` — файлы локально, не пушатся
- Граф обновлён после всех правок дня командой `graphify update .`

---

### T63 — Обновить справку (значок «?») под новые кнопки/функции `TODO` (2026-07-09)
**Файл:** `src/components/HelpModal.tsx` (секции `CHATS_LIST_SECTION`, `CLIENT_CARD_SECTION`, `INVENTORY_SECTION_ADMIN`, `INVENTORY_SECTION_MANAGER`, `CASH_SECTION`)

**Что накопилось необновлённым с прошлой ревизии справки** (см. [[feature_help_modal_expanded]] в памяти — принцип "каждая кнопка каждой страницы задокументирована"):
- Кнопка «Создать контакт» в шапке «Чаты» — раньше была карандаш-иконка без подписи (T58)
- Верхний поиск на «Чаты» теперь находит и клиентов без чата, не только существующие чаты (T58)
- «Удалить контакт» в информации о чате — admin/branch_admin (T58)
- Вкладка «История заявок» на Складе — новая, для обычных менеджеров (read-only статус своих заявок) (T55)
- Бейдж «статус моей заявки изменился» — что означает, когда гаснет (T56)
- Кнопки «Редактировать»/«Удалить» в списке «Приходы» — теперь доступны и менеджеру филиала «Склад», не только admin/branch_admin (T61)
- Разбивка Halyk/Kaspi-перевод в отчёте «Касса» (админ, по филиалам) (T55)

**Перед началом:** сверить с TASKS.md — набегут ещё правки, пока T62 не взят в работу; собрать актуальный список перед началом, не полагаться только на этот список.

---

### T62 — Ролевая видимость вкладок склада + категории-фильтры как в «Товарах» `TODO` (2026-07-09)
**Описание (с голоса пользователя, требует уточнения при взятии в работу):**

1. **Разграничить видимость вкладок «Склад» по ролям.** Сейчас нужно свести таблицу: у кого какая роль (admin/branch_admin/manager/склад) видит какие вкладки (Обзор, Товары, Движения, Приходы, Продажи, Списания, Возвраты, Ревизии, Этикетки, Заявки) — и решить, кому что реально нужно. Пример от пользователя: "Движения" многим не нужны — возможно, стоит скрыть у части ролей. Не путать с T61 (там разбирали доступ к кнопкам Редактировать/Удалить в Приходах для склада) — здесь про сами вкладки целиком.

2. **Добавить категории-фильтры под поиском** на другие вкладки — по образцу того, что уже есть в «Товарах» (чипы под строкой поиска: Все / Аксессуары / Готовые очки / Капли и т.д., группировка по `product_category`). Пользователь не уточнил, на какие именно вкладки это добавить (Приходы? Продажи? Списания?) — уточнить при взятии в работу.

**Перед началом:** переспросить пользователя, что именно должно измениться в видимости вкладок (конкретно по ролям) и на какие вкладки добавить категории-фильтры — сообщение было надиктовано вкратце, "чтобы не забыть", без детальной проработки.

---

### T23 — Подписка: напоминание об оплате и блокировка приложения `TODO`
**Описание:** Функция абонентской поддержки — ежемесячный платёж. Дата задаётся вручную администратором.

**Поведение:**
- За 5 дней до срока → жёлтый баннер «Оплата через N дней»
- В день срока → красный баннер «Оплата сегодня»
- Срок прошёл, не оплачено → полноэкранная заглушка, приложение заблокировано

**Что делать:**
1. SQL-миграция: таблица `subscription` (due_date, is_paid, paid_at, period_label)
2. Сервисные функции в `inventory.ts` или отдельный `subscription.ts`
3. Хук `useSubscription` — проверка статуса при загрузке
4. Компонент баннера предупреждения (жёлтый / красный)
5. Компонент полноэкранной блокировки
6. Раздел в настройках для admin: установить дату + кнопка «Отметить как оплачено»

**Важно:**
- Блокировка только фронтенд (для внутреннего инструмента достаточно)
- Активируется только когда admin сам установит первую дату — на существующих пользователях не скажется
- Можно делать параллельно с работающим приложением

**Оценка:** ~3 часа

---

### T48 — Редактирование количества в одобренной заявке на склад `DONE` (2026-07-06)
**Файлы:** `src/pages/InventoryPage.tsx`, `src/services/inventory.ts`

**Что сделано:**
- В карточке заявки со статусом «Одобрена» количество каждой позиции теперь редактируемое поле (было — просто текст)
- Новая функция `updateStockRequestItemQuantities()` сохраняет правки в `stock_request_items` перед вызовом `shipStockRequest`
- `shipStockRequest()` пропускает позиции с количеством 0 (способ исключить позицию из отгрузки, не отправляя лишнее)

**Найденный баг (RLS):** на `stock_request_items` не было политики `UPDATE` вообще — только SELECT/INSERT. `PATCH` возвращал `204` (успех), но фактически 0 строк обновлялось — классическая ловушка PostgREST/RLS. Добавлена политика `employees_update_request_items`.

**Побочная находка:** политика `admin_update_requests` на `stock_requests` (T45) на проде оказалась НЕ заменена — по факту всё ещё ограничена `admin`/`branch_admin`, хотя T45 утверждал обратное. В списке миграций прода фикса не было. Добавлена отдельная политика `warehouse_staff_update_requests` (через новую функцию `get_user_branch_id()`) — сотрудники филиала «Склад» любой роли теперь могут одобрять/отправлять заявки.

**Проверено:** end-to-end в тестовом окружении (создание заявки → одобрение → правка количества 1→7 → отправка → количество сохранилось), затем применено на прод.

---

### T53 — Баг-хантинг реальным тестированием: фильтр категорий, scope товаров, разбивка оплаты, tsc `DONE` (2026-07-06)
**Файлы:** `src/services/inventory.ts`, `tsconfig.app.json`, `src/components/Inventory/CashSessionCard.tsx`, `src/services/cashAdmin.ts`, `src/components/Admin/AdminCashView.tsx`

**Найдено при реальном тестировании (создание прихода/перемещения, оформление продажи):**
1. Фильтр по категории в поиске товара (Новая продажа) показывал пусто при выборе любой категории — `getProductsFromStock()` не запрашивал `category_id` у товара (только вложенный `category`), фильтр `p.category_id === selectedCategory` не совпадал никогда. Добавлено поле в select.
2. Рядом: `getProducts()` принимал 0 параметров, но вызывался с `activeBranchId` — аргумент молча игнорировался, не-admin видел товары всех филиалов во вкладке «Товары», а не только своего. Добавлен параметр `branchId?`.
3. **Критичная находка про сам процесс проверки:** `npx tsc --noEmit` весь проект не проверял (solution-конфиг без `--build`) — все "типы чистые" в отчётах были фикцией. Починено (`tsconfig.app.json` + `ignoreDeprecations`), правильная команда — `npx tsc --noEmit --project tsconfig.app.json`. Обнаружено ~30 предсуществующих ошибок в других местах — не трогали, отдельная задача на будущее.
4. Продажа с оплатой Kaspi-перевод/Halyk падала с `sales_payment_method_check` — constraint на проде разрешал только `cash/kaspi_qr/mixed`. **SQL дан пользователю, ждёт применения на тесте.**
5. Касса (карточка менеджера и отчёт админа) считала Halyk/Kaspi-перевод как «наличные» и включала в сумму «к сдаче» — по факту это банковские переводы, физически в кассе их нет. Разделено: `system_cash` = только `paid_cash`, Halyk/Kaspi-перевод — отдельные информационные строки в обеих точках (карточка + админ-отчёт), `system_total`/«Итого» по-прежнему включает всё.

---

### T52 — Переоткрытие кассы в тот же день + история закрытий `DONE` (2026-07-06)
**Файлы:** `src/services/cashSessions.ts`, `src/components/Inventory/CashSessionCard.tsx`, `src/services/cashAdmin.ts`, `src/components/Admin/AdminCashView.tsx`, миграция (новая таблица + RPC)

**Контекст:** T49 сделало закрытие кассы финальным на день (как и было в исходной `close_cash_session`) — но менеджер может закрыть по ошибке или рано, а потом нужно обслужить ещё клиента.

**Что сделано:**
- Кнопка «Переоткрыть кассу» — доступна любому менеджеру своего филиала (не только admin — admin вообще не видит этот экран, у него отдельный отчёт «Касса»)
- RLS не разрешает не-admin обновлять уже закрытую сессию → переоткрытие идёт через новую `SECURITY DEFINER` RPC `reopen_cash_session` (та же схема, что `close_cash_session`), проверяет права через `get_user_branch_id()`
- Новая таблица `cash_session_closures` — append-only лог каждого закрытия (снимок сумм на момент закрытия); переоткрытие её не трогает, только сбрасывает текущую сессию
- Блок «Предыдущие закрытия сегодня» — и в карточке менеджера, и в отчёте админа (последнее закрытие не дублируется — оно уже видно в основном блоке «Итоги закрытия»)

**SQL-миграция (применена на тесте 2026-07-06; статус на проде — ПРОВЕРИТЬ, см. "ПЕРЕДАЧА СЕССИИ" вверху файла):**
```sql
CREATE TABLE cash_session_closures (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id uuid NOT NULL REFERENCES cash_sessions(id),
  branch_id uuid NOT NULL REFERENCES branches(id),
  closed_at timestamptz NOT NULL DEFAULT now(),
  system_cash numeric NOT NULL,
  system_kaspi numeric NOT NULL,
  system_total numeric NOT NULL,
  actual_cash numeric NOT NULL,
  cash_discrepancy numeric NOT NULL,
  notes text,
  closed_by uuid REFERENCES employees(id)
);

ALTER TABLE cash_session_closures ENABLE ROW LEVEL SECURITY;

CREATE POLICY "employees_read_cash_session_closures" ON cash_session_closures
  FOR SELECT
  USING (get_user_role() = 'admin' OR branch_id = get_user_branch_id());

CREATE OR REPLACE FUNCTION public.reopen_cash_session(p_session_id uuid)
RETURNS void
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
  v_session cash_sessions%ROWTYPE;
BEGIN
  SELECT * INTO v_session FROM cash_sessions WHERE id = p_session_id;
  IF NOT FOUND THEN RAISE EXCEPTION 'Сессия не найдена'; END IF;
  IF v_session.status != 'closed' THEN RAISE EXCEPTION 'Касса уже открыта'; END IF;
  IF v_session.date != CURRENT_DATE THEN RAISE EXCEPTION 'Можно переоткрыть только сегодняшнюю кассу'; END IF;
  IF get_user_role() != 'admin' AND get_user_branch_id() != v_session.branch_id THEN
    RAISE EXCEPTION 'Недостаточно прав для переоткрытия кассы этого филиала';
  END IF;

  UPDATE cash_sessions SET
    status = 'open',
    actual_cash = NULL,
    cash_discrepancy = NULL,
    closed_at = NULL,
    notes = NULL
  WHERE id = p_session_id;
END;
$$;

-- close_cash_session дополнительно пишет снимок в историю при каждом закрытии:
CREATE OR REPLACE FUNCTION public.close_cash_session(p_session_id uuid, p_actual_cash numeric, p_employee_id uuid, p_notes text DEFAULT NULL::text)
RETURNS void
LANGUAGE plpgsql
SECURITY DEFINER
AS $function$
DECLARE
  v_session       cash_sessions%ROWTYPE;
  v_cash_expenses NUMERIC;
  v_expected_cash NUMERIC;
  v_discrepancy   NUMERIC;
BEGIN
  SELECT * INTO v_session FROM cash_sessions WHERE id = p_session_id;
  IF NOT FOUND THEN RAISE EXCEPTION 'Сессия не найдена'; END IF;
  IF v_session.status = 'closed' THEN RAISE EXCEPTION 'Касса уже закрыта'; END IF;

  SELECT COALESCE(SUM(amount), 0) INTO v_cash_expenses
  FROM expenses
  WHERE branch_id      = v_session.branch_id
    AND date           = v_session.date
    AND payment_method = 'cash';

  v_expected_cash := v_session.system_cash - v_cash_expenses;
  v_discrepancy   := v_expected_cash - p_actual_cash;

  UPDATE cash_sessions SET
    actual_cash      = p_actual_cash,
    cash_discrepancy = v_discrepancy,
    status           = 'closed',
    notes            = p_notes,
    closed_at        = NOW()
  WHERE id = p_session_id;

  INSERT INTO cash_session_closures (
    session_id, branch_id, closed_at, system_cash, system_kaspi, system_total,
    actual_cash, cash_discrepancy, notes, closed_by
  ) VALUES (
    p_session_id, v_session.branch_id, NOW(), v_session.system_cash, v_session.system_kaspi, v_session.system_total,
    p_actual_cash, v_discrepancy, p_notes, p_employee_id
  );

  IF ABS(v_discrepancy) > 0 THEN
    INSERT INTO watchlist_events (
      type, branch_id, employee_id, quantity, amount, notes, extra
    ) VALUES (
      'cash_discrepancy',
      v_session.branch_id,
      p_employee_id,
      1,
      ABS(v_discrepancy),
      'Кассовое расхождение: ожидалось ' || v_expected_cash ||
        ' ₸ (продажи ' || v_session.system_cash ||
        ' - расходы ' || v_cash_expenses ||
        '), сдано ' || p_actual_cash || ' ₸',
      jsonb_build_object(
        'system_cash',   v_session.system_cash,
        'cash_expenses', v_cash_expenses,
        'expected_cash', v_expected_cash,
        'actual_cash',   p_actual_cash,
        'discrepancy',   v_discrepancy,
        'date',          v_session.date
      )
    );
  END IF;
END;
$function$;
```

---

### T51 — Возвраты перенесены в Магазин, найден и исправлен баг с зависанием вкладки `DONE` (2026-07-06)
**Файлы:** `src/App.tsx`, `src/pages/InventoryPage.tsx`

**Контекст:** Продажи уже были скрыты из общего «Склад» для не-admin (видны только через «Магазин»), а Возвраты — нет, оставались доступны и напрямую через «Склад». Добавил «Возвраты» как под-раздел «Магазина» (рядом с Продажи/Предзаказы), скрыл из «Склад» для не-admin тем же способом, что и Продажи.

**Попутно найден реальный баг:** `InventoryPage`'s `tab` состояние читало проп `defaultTab` только один раз через `useState`-инициализатор. При переключении под-вкладок в «Магазине» React переиспользует тот же смонтированный экземпляр компонента — смена `defaultTab` пропа никак не пересинхронизировала `tab`. Плюс блок «Продажи» рендерился по условию `tab === 'sales' || storefront` — то есть показывался всегда в storefront-режиме, что бы ни было в `tab`. Из-за этого «Возвраты» физически не могли отобразиться — всегда перекрывались «Продажами».
**Исправление:** добавлен `useEffect`, синхронизирующий `tab` с `defaultTab` при изменении пропа; убран `|| storefront` из условия рендера «Продажи».

**Проверено вживую в тестовом окружении** (Джандосова/Гум): касса «Не открыта» → «Открыть кассу» корректно работает на филиале без существующей сессии; продажа блокируется без открытой кассы; переключение Продажи↔Возвраты в Магазине работает; у admin в «Склад» Продажи/Возвраты/Этикетки/Заявки остались нетронуты.

---

### T50 — Магазин/Касса не для склада, Этикетки только для склада `DONE` (2026-07-06)
**Файлы:** `src/App.tsx`, `src/pages/InventoryPage.tsx`

**Контекст:** после T49 обнаружилось, что кнопка «Магазин» (продажи + касса) была скрыта только для филиала «Мастерская» — склад ошибочно получал тот же доступ к продажам/кассе, что и розничные филиалы, хотя со складом никто не работает по кассе (только заявки).

**Что сделано:**
- Кнопка «Магазин» (desktop-нав менеджера + нижняя мобильная навигация) теперь скрыта и для «Мастерской», и для «Склад» — `employee?.branch_id !== WORKSHOP_BRANCH_ID && employee?.branch_id !== WAREHOUSE_ID`
- Вкладка «Этикетки» в `InventoryPage` теперь видна только admin и складу (`canSeeLabelsTab`, тот же паттерн что у `canSeeRequestsTab`) — розничным филиалам она не нужна (принтер физически только на складе)

**Осознанно не в скоупе:** вкладки «Продажи»/«Возвраты» внутри самого модуля «Склад» (не через «Магазин») пока не скрыты для склада — если нужно закрыть и эту лазейку, отдельная небольшая правка.

---

### T49 — Явное открытие/закрытие кассы (вместо всегда открытой) `DONE` (2026-07-06)
**Файлы:** `src/services/cashSessions.ts` (новый), `src/components/Inventory/CashSessionCard.tsx`, `AddSaleModal.tsx`, `AddExpenseModal.tsx`

**Было:** кассовая сессия создавалась автоматически (status=open) при первой загрузке вкладки «Магазин» — по факту касса была всегда открыта, без явного действия менеджера в начале дня.

**Стало:**
- `CashSessionCard` больше не создаёт сессию сама — если на сегодня записи нет, показывает состояние «Не открыта» с кнопкой «Открыть кассу»
- Продажи (`AddSaleModal`) и расходы за сегодняшнюю дату (`AddExpenseModal`) блокируются с понятным алертом, если касса филиала не открыта
- БД/RLS не менялись — уникальный констрейнт `(branch_id, date)` и политики на `cash_sessions` уже подходили

**Осознанно не в скоупе:** предоплаты/доплаты мастерской (`service_orders`) тоже попадают в подсчёт кассы, но их приём (WorkshopPage) пока не блокируется — не было явного запроса, отмечено как известный пробел.

---

### T46 — Индикаторы непрочитанного (бейдж на иконке PWA + точки в списках) `DONE` (2026-07-05)
**Описание:** Найдено при тестировании push-уведомлений в тестовом окружении — бейдж на иконке PWA не держался, даже когда реально было непрочитанное.

**Что оказалось:**
- Точки непрочитанного в чатах (per-chat `unread_count`, пропадают при открытии конкретного чата) уже работали корректно — как в WhatsApp, менять не пришлось.
- Реальный баг — в [main.tsx:17](src/main.tsx:17): при **любом** сворачивании/разворачивании приложения бейдж на иконке принудительно обнулялся (`navigator.clearAppBadge()`), независимо от того, прочитано что-то или нет.

**Что сделано:**
- Убран преждевременный `clearAppBadge()` из `main.tsx` — бейдж теперь считается только по реальному непрочитанному (`totalBadge` в App.tsx), держится пока не прочитано, как в WhatsApp
- Заявки на склад (`status IN ('new','approved')`) добавлены в общий `totalBadge` для admin/склад — раньше учитывались только внутри вкладки «Заявки», на иконку PWA не влияли

---

### T67 — Улучшенный (неразрушающий) клемп предоплаты мастерской/предзаказа `DONE` (2026-08-04)
**Файлы:** `src/utils/formatters.ts`, `src/components/Inventory/AddSaleModal.tsx`, `src/components/Workshop/AddServiceOrderModal.tsx`

**Проблема со старым клемпом (T?? от 2026-08-03, коммит 3e43bc1):** реактивный `useEffect` на изменение стоимости услуги/запчастей форсированно урезал уже введённую предоплату при КАЖДОМ изменении цены. Если оператор сначала вводил предоплату, а потом перепечатывал стоимость услуги — предоплата необратимо занижалась без явного предупреждения.

**Новый паттерн (применён во всех трёх местах ввода предоплаты):**
- `clampPrepayment(value, total) = Math.max(0, Math.min(value, total))` — общий helper в `formatters.ts`, с юнит-тестами
- `onChange` поля предоплаты сохраняет введённое число как есть, БЕЗ клемпа
- `onBlur` — применяет клемп (но не стирает значение, если `total <= 0` — клемпить нечем, просто нет ещё цены; иначе поле тихо стиралось при уходе с фокуса раньше ввода стоимости — тот же класс бага, что чинили)
- Единая derived-переменная `prepaymentExceedsTotal` — красная подсказка + блокировка кнопки submit
- В `handleSubmit` — guard (алерт, не отправлять) + финальный `clampPrepayment` перед сохранением как двойная защита

**Где применено:**
1. `AddSaleModal.tsx` — предоплата мастерской внутри продажи (`workshopPrepayment`, исходное место бага)
2. `AddSaleModal.tsx` — предоплата предзаказа (`prepaymentAmount`, режим `mode==='preorder'`)
3. `AddServiceOrderModal.tsx` — прямое создание заказа мастерской (`prepayment`)

**Тесты:** `src/test/formatters.test.ts` (юнит на clampPrepayment, границы, total<=0), `src/test/addSaleModal.prepayment.test.tsx` (10 тестов, включая реальный submit с проверкой payload и сценарий «предоплата → перепечатка стоимости → не испортилась» для обоих мест в AddSaleModal), `src/test/addServiceOrderModal.prepayment.test.tsx` (3 теста, то же для прямого заказа мастерской). Вручную проверено: временный возврат старого деструктивного `useEffect` ломает тест — подтверждает, что тест реально ловит регрессию.

**Ревью:** независимый саб-агент (opus) — критичных проблем не найдено. Найден и исправлен один средний риск (тихое стирание предоплаты на blur при total=0). Отмечено, но НЕ исправлено (вне скоупа задачи): в `CashSessionCard.tsx` расчёт остатка по мастерской (`service_price + parts_price - prepayment`) без `Math.max(0, ...)` — при завышенной предоплате может уйти в минус; отдельный клемп на бэкенде (`createServiceOrder`/`createOrder`) как вторая линия защиты не добавлен (сейчас единственные писатели этих полей — три отревьюенные модалки).

---

## Статистика
- Всего: 80 задач
- TODO: 4 (T23, T62, T63, T77)
- IN_PROGRESS: 2 (T65 — печать этикеток, активная отладка; T66 — заявки на склад, ждёт проверки на проде)
- DONE: 76
- SKIP: 0

## Исключено (Kaspi — не запущен)
- Kaspi QR ручное подтверждение без оплаты
- handleKaspiConfirm без проверки ошибки
- saleReturnsCash считает Kaspi как наличные
- Pending-продажа при закрытии браузера (только Kaspi-флоу)
